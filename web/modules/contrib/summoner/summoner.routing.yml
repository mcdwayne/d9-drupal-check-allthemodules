summoner.load:
  path: '/summoner/load/{libraries}'
  defaults:
    _content: '\Drupal\summoner\Controller\SummonerController::load'
  requirements:
    _access: 'TRUE'
  options:
    parameters:
      libraries:
        type: 'summoner:libraries'
