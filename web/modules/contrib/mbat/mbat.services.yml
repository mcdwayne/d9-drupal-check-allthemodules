services:
  mbat.breadcrumb:
    class: Drupal\mbat\MbatBreadcrumbBuilder
    arguments: ['@menu.active_trail', '@plugin.manager.menu.link', '@config.factory', '@router.admin_context']
    tags:
      # The priority must be higher than core taxonomy builder (priority: 1002)
      - { name: breadcrumb_builder, priority: 1010 }
