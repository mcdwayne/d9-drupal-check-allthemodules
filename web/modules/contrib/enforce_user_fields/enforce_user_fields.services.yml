services:
  enforce_user_fields.event_subscriber:
    class: Drupal\enforce_user_fields\EventSubscriber\EnforceUserFieldsSubscriber
    arguments: [ '@current_user', '@current_route_match', '@request_stack', '@messenger' ]
    tags:
      - { name: event_subscriber }
