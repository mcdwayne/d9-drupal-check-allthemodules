fs:
  docroot: .machinery

#
# > run-drupal.sh site:install nodeletter_profile --site-name "Nodeletter" --langcode en --db-type mysql --db-host db --db-name drupal --db-user root --db-pass root --account-name admin --account-pass admin --force --no-interaction
#