(function ($, Drupal) {

  'use strict';

  Drupal.behaviors.background_videos = {
    attach: function (context, settings) {

      //var config = settings.background_video;
      //console.log(config);
    }
  };
})(jQuery, Drupal);
