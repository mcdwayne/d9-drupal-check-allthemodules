services:
  linkcker.command:
    class: Drupal\linkchecker\Commands\LinkCheckerCommands
    arguments:
      - '@config.factory'
      - '@logger.channel.linkchecker'
    tags:
      - { name: drush.command }
