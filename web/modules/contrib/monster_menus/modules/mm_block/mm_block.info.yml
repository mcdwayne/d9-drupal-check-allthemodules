name: 'MM Block Contents'
description: 'Displays the contents of a block as a node, which allows the block to appear on a single page, rather than on every page using a particular theme.'
dependencies:
package: 'Monster Menus'
core: 8.x
type: module
