linkedin_oauth.settings:
  type: config_object
  label: 'LinkedIn OAuth settings'
  mapping:
    api_key:
      type: string
      label: 'API key (Client ID)'
    api_secret:
      type: string
      label: 'API secret (Client Secret)'
    redirect_path:
      type: string
      label: 'Redirect path'
