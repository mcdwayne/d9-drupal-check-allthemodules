micro_taxonomy.settings:
  type: config_object
  label: 'Micro taxonomy settings'
  mapping:
    vocabularies:
      type: sequence
      label: 'The vocabularies we can associate with a site entity'
      sequence:
        type: string
