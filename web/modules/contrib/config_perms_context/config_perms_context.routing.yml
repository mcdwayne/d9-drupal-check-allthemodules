
config_perms_context.settings:
  path: '/admin/config/development/config_perms_context'
  defaults:
    _form: '\Drupal\config_perms_context\Form\Settings'
    _title: 'Custom Permissions Context'
  requirements:
    _permission: 'administer site configuration'
  options:
    _admin_route: TRUE
