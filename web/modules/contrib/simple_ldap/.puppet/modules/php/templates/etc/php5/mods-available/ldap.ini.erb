;; THIS FILE IS MANAGED BY PUPPET
;; modules<%= template_source.gsub(Regexp.new("^#{Puppet::Node::Environment.current[:modulepath].gsub(':','|')}"),"") %>

; configuration for php LDAP module
; priority=20
extension=ldap.so
<% if @max_links %>ldap.max_links=<%= @max_links %><% end -%>

