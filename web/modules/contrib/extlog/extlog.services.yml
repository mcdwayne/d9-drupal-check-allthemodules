services:
  logger.extlog:
    class: Drupal\extlog\Logger\ExternalLog
    arguments: ['@config.factory', '@logger.log_message_parser']
    tags:
      - { name: logger }
      - { name: backend_overridable }