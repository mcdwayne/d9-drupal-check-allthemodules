# Administrative page
firebase.configuration:
  path: '/admin/config/system/firebase'
  defaults:
    _form: '\Drupal\firebase\Form\ConfigurationForm'
    _title: 'Firebase Push Notification Configuration'
  requirements:
    _permission: 'administer site configuration'
