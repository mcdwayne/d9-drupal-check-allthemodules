# Schema for the views plugins of the Views Default Arguments module.

views.argument_default.entity_reference:
  type: sequence
  label: 'Content ID from Node Entity Reference'
  sequence:
    type: string
    label: 'Nid'
