upgrade_tool.config:
  type: config_object
  label: 'Upgrade tool config'
  mapping:
    mode:
      type: text
      label: 'Upgrade tool mode'
    features:
      type: sequence
      label: 'Features list'
      sequence:
        type: string
