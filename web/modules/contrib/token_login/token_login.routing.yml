token_login.token_login_configuration:
  path: '/admin/config/people/token_login'
  defaults:
    _form: '\Drupal\token_login\Form\TokenLoginConfiguration'
    _title: 'Token Login Settings'
  requirements:
    _permission: 'access administration pages'
