name: Token login
type: module
description: Replace the default password based authentication mechanism with one-off login tokens.
core: 8.x
package: Other
configure: token_login.token_login_configuration
