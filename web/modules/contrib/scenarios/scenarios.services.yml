services:
  scenarios_handler:
    class: Drupal\scenarios\ScenariosHandler
    arguments: ["@module_handler", "@module_installer", "@info_parser", "@plugin.manager.migration", "@config.factory", "@default_content_deploy.importer"]
