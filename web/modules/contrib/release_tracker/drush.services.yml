services:
  release_tracker.commands:
    class: \Drupal\release_tracker\Commands\ReleaseTrackerCommands
    arguments:
      - '@release_tracker.release_tracker'
    tags:
      - { name: drush.command }
