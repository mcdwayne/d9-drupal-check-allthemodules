field_presets.settings:
  type: config_object
  label: 'Field presets settings'
  mapping:
    form_display:
      type: integer
      label: 'Form display enabled'
    redirect_default:
      type: integer
      label: 'Default redirect enabled'
