webform_submit:
  label: 'Webform submit'
  category: 'A Webform'
  context:
    webform_fields:
      type: 'webform_fields'
      label: 'Webform fields'
    webform_info:
      type: 'webform_info'
      label: 'Webform info'

updating_submission:
  label: 'Updating a submission'
  category: 'A Webform'
  context:
    webform_fields:
      type: 'webform_fields'
      label: 'Webform fields'
    webform_info:
      type: 'webform_info'
      label: 'Webform info'

deleting_submission:
  label: 'Deleting a submission'
  category: 'A Webform'
  context:
    webform_fields:
      type: 'webform_fields'
      label: 'Webform fields'
    webform_info:
      type: 'webform_info'
      label: 'Webform info'  

viewing_submission:
  label: 'Viewing a submission'
  category: 'A Webform'
  context:
    webform_fields:
      type: 'webform_fields'
      label: 'Webform fields'
    webform_info:
      type: 'webform_info'
      label: 'Webform info'
