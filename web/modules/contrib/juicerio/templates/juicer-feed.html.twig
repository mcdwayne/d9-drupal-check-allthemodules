{#
/**
 * @file
 * Default theme implementation to display a juicer social feed.
 *
 * @ingroup themeable
 */
#}
{{ attach_library('juicerio/juicer.juicerio') }}
<ul class="juicer-feed" {%  if feed_id %}data-feed-id="{{ feed_id }}"{% endif %} {%  if post_num %}data-per="{{  post_num }}"{% endif %} {%  if infinite_pages %}data-pages="{{ infinite_pages }}"{% endif %} {%  if gutter_amt %}data-gutter="{{ gutter_amt }}"{% endif %} {%  if column_num %}data-columns="{{ column_num }}"{% endif %} {%  if filters %}data-filter="{{ filters }}"{% endif %}></ul>