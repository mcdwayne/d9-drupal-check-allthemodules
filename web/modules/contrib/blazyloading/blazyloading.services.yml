services:
  blazyloading.blazy_loading_subscriber:
    class: Drupal\blazyloading\EventSubscriber\BlazyLoadingSubscriber
    arguments: ['@config.factory', '@current_user', '@entity.manager']
    tags:
      - { name: event_subscriber }
