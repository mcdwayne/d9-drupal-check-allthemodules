# Schema for the views experience filter plugins.

views.filter.experience:
  type: views_filter
  label: 'Experience filter'

views.filter_value.experience:
  type: mapping
  label: 'Numeric'
  mapping:
    min:
      type: string
      label: 'Min'
    max:
      type: string
      label: 'And max'
    value:
      type: string
      label: 'Value'
