<?php

function layouts_preprocess_layout(&$variables) {
  if ($variables['theme_hook_original'] == 'layout__hero') {
    $variables['hero_attributes'] = $variables['content']['#hero_attributes'];
  }
}
