langcode: en
status: true
dependencies:
  enforced:
    module:
      - guestbooks # This is the name of the module we're using for this content type.  If we don't add this dependency, Drupal will not remove the content type when we uninstall our module.
  module:
    - menu_ui
third_party_settings:
  menu_ui:
    available_menus: {  }
    parent: ''
name: Guestbooks
type: guestbooks
description: 'Track Guestbooks Entries for Site.'
help: ''
new_revision: false
preview_mode: 0
display_submitted: true
