services:
  rollback.update_rollback:
    class: Drupal\rollback\Command\RollbackCommand
    arguments: ['@state', '@console.chain_queue', '@rollback.rollback']
    tags:
      - { name: drupal.command }
