services:
  rollback.register:
    class: Drupal\rollback\Register
    arguments: ['@database', '@datetime.time']
  rollback.rollback:
    class: Drupal\rollback\Rollback
    arguments: ['@database', '@datetime.time']