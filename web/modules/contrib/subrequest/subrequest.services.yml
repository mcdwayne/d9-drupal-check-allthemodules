services:
  subrequest.manager:
    class: Drupal\subrequest\SubrequestManager
    arguments: ['@request_stack', '@router', '@controller_resolver', '@current_route_match', '@logger.channel.subrequest']
