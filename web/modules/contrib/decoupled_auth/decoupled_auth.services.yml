services:
  # Defines a service for acquiring users.
  decoupled_auth.acquisition:
    class: Drupal\decoupled_auth\AcquisitionService
    arguments: ['@config.factory', '@entity_type.manager', '@event_dispatcher']
    shared: false
