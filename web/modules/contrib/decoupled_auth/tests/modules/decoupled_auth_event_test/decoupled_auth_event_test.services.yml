services:
  # Defines a service for acquiring users.
  decoupled_auth_event_test.test_subscriber:
    class: Drupal\decoupled_auth_event_test\EventSubscriber\DecoupledAuthEventTestSubscriber
    tags:
      - { name: event_subscriber }
