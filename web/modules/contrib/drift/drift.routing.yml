drift.config:
  path: '/admin/config/services/drift'
  defaults:
    _form: '\Drupal\drift\Form\DriftSettingsForm'
    _title: 'Drift Configuration'
  requirements:
    _permission: 'administer site configuration'