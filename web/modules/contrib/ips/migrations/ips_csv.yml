langcode: en
status: true
id: ips_csv
label: IPs(csv)
migration_tags: { }
source:
  plugin: csv
  header_row_count: 1
  path: modules/idcp/cbos/ips/tests/data/ips.csv
  keys:
    - name
    - type
process:
  type:
    plugin: entity_generate
    entity_type: ips_type
    value_key: type
  name: name
destination:
  plugin: entity:ips
migration_dependencies: {}
