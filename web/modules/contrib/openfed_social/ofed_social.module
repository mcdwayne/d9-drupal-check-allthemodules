<?php

/**
 * @file
 * Module to provide social share links for Openfed distribution.
 */

/**
 * Implements hook_theme().
 */
function ofed_social_theme($existing, $type, $theme, $path) {
  return [
    // Default theme, using this module styles and images.
    'ofed_social_links_default' => [
      'variables' => [
        'icon_links' => [],
      ],
    ],
    // Administration form theming.
    'ofed_social_admin_form' => [
      'render element' => 'form',
      'file' => 'includes/ofed_social.theme.inc',
    ],
  ];
}

