dialect.shared_block_config_form:
  path: '/admin/config/regional/dialect'
  defaults:
    _form: '\Drupal\dialect\Form\SharedBlockConfigForm'
    _title: 'Dialect language selectors'
  requirements:
    _permission: 'administer dialect configuration'
  options:
    _admin_route: TRUE
