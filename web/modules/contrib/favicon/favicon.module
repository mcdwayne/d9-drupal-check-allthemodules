<?php

/**
 * @file
 * Module file for favicon module.
 */

/**
 * Implements hook_menu().
 */
function favicon_menu() {
  $items['favicon.ico'] = array(
    'title' => 'Favicon',
    'description' => "Override favicon.ico to forward to the theme's shortcut icon.",
    'route_name' => 'favicon.shortcut_icon',
  );

  return $items;
}
