* Contextual links op het block
* Block settings for CT ipv per node (view mode as block).
* Entity level blocks integratie?
* Block test.
** Testen van block weergave.
* JS / title support voor andere entity types <-- user
* auto vullen met token?
