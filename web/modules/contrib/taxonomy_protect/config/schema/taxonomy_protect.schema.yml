# Schema for the configuration file of the taxonomy_protect module.

taxonomy_protect.settings:
  type: config_object
  label: 'Taxonomy Protect settings'
  mapping:
    taxonomy_protect_vocabularies:
      type: sequence
      label: 'Vocabularies to protect'
      sequence:
        type: string
        label: 'Vocabulary'
