services:
  sitemap.helper:
    class: Drupal\sitemap\SitemapHelper
    arguments: ['@config.factory']
  plugin.manager.sitemap:
    class: Drupal\sitemap\SitemapManager
    parent: default_plugin_manager
