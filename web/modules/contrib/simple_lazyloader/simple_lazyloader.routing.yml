simple_lazyloader.admin_settings:
  path: '/admin/config/simple_lazyloader'
  defaults:
    _form: '\Drupal\simple_lazyloader\Form\LazyloaderConfigForm'
    _title: 'Lazyloader configuration'
  requirements:
    _permission: 'administer site configuration'
