invoice_agent.config:
  path: 'admin/commerce/config/invoice_agent'
  defaults:
    _form: '\Drupal\invoice_agent\Form\ConfigForm'
    _title: Invoice Agent Configuration
  requirements:
    _permission: administer invoice agent
