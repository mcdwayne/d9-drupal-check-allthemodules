# Schema for the configuration files of the module
# language_negotiator_content_entity_all_routes.

language_negotiator_content_entity_all_routes.settings:
  type: config_object
  label: 'Settings of the language negotiation method "Content language (all routes)"'
  mapping:
    strict:
      type: boolean
      label: 'Strict mode will enforce outbound entity URLs to have the language specified'
