services:
  healthcheck_email.check_subscriber:
    class: Drupal\healthcheck_email\EventSubscriber\EmailCheckSubscriber
    arguments: ['@plugin.manager.mail', '@token', '@config.factory']
    tags:
      - { name: event_subscriber }
