healthcheck.check.config_test:
  type: config_entity
  label: 'Check config test'
  mapping:
    id:
      type: string
      label: 'ID'
    label:
      type: label
      label: 'Label'
    uuid:
      type: string
    healthcheckConfig:
      type: healthcheck.healtcheck.plugin.[%parent.id]
      label: 'Healthcheck plugin configuration'

healthcheck.healtcheck.plugin.config_test:
  type: mapping
  label: 'Configurable Healthcheck plugin configuration'
  mapping:
    status:
      type: string
      label: 'Status'
