stripe_webform.webhook:
  label: 'A hook for a webform submitted has been received'
  category: 'Stripe'
  context:
    type:
      type: 'string'
      label: 'Stripe event type'
    webform_submission:
      type: 'entity:webform_submission'
      label: 'Webform'
