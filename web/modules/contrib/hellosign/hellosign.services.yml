services:
  hellosign:
    class: Drupal\hellosign\Hellosign
    arguments: ['@config.factory', '@encryption', '@file_system', '@logger.channel.hellosign', '@string_translation']
  logger.channel.hellosign:
    parent: logger.channel_base
    arguments: ['hellosign']
