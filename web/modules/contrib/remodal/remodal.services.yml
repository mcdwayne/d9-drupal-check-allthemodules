services:
  remodal_main_content_renderer.remodal:
    class: Drupal\remodal\Render\MainContent\RemodalRenderer
    arguments: ['@title_resolver']
    tags:
      - { name: render.main_content_renderer, format: drupal_remodal }
