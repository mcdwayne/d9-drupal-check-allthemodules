services:
  password_change_rules.enforce_change:
    class: Drupal\password_change_rules\EventSubscriber\EnforcePasswordChange
    arguments: ['@current_user', '@entity_type.manager']
    tags:
      - { name: event_subscriber }
