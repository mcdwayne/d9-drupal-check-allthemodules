password_change_rules.settings:
  type: config_object
  label: 'Password Change Rules settings'
  mapping:
    change_password_message:
      type: label
      label: 'Password Change Message'
    admin_registered_account:
      type: boolean
      label: 'Force reset if admin registers the account'
    admin_change_password:
      type: boolean
      label: 'Force reset if admin changes account password'
