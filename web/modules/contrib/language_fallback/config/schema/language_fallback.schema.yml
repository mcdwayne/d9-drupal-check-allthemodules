# Schema for configuration files of the Sublanguage module.

language.entity.*.third_party.language_fallback:
  type: mapping
  label: 'Per language fallback settings'
  mapping:
    fallback_langcode:
      type: string
      label: 'Fallback language code'
