services:
  cascading_deletion.manager:
    class: Drupal\cascading_deletion\CascadingDeletionManager
    arguments: ['@language_manager', '@cache.default', '@entity_field.manager', '@entity_type.manager', '@entity_field.manager']