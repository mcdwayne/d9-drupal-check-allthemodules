# Schema for the configuration files of the Trending Images module.

trending_images.settings:
  type: config_object
  mapping:
    api_key:
      type: string
      label: 'API Key'
    langcode:
      type: string
      label: 'Language code'
