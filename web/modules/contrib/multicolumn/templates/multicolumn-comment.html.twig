{#
/**
 * @file
 * Default theme implementation for multicolumn comment.
 *
 * Available variables:
 * - text:  the text to be wrapped in a comment.
 *
 * @ingroup themeable
#}
{%- if text -%}
  <!-- {{ text }} -->
{%- else -%}
  {{- empty -}}
{%- endif -%}
