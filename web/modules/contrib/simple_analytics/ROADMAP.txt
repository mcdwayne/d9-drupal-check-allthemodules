TODO List:
- Handle "CLOSE" Tracking for windows closing.

- Write tests for 'archive'

- Use sessoion to detect user visit / unique visitor

- Write tests for exclude urls.

- CE cookie complient (eu_cookie_compliance)
- Privacy message.
- CE / Privacy settings page
- Store in config.

- Show (3 most) referers -> Page -> Exit page schema.

- Notifications (If event ...)

- Add do not track.

- Most visited users ??, urls ...
- Top urls

- Get the title in server side : like :
$request = \Drupal::request();
if ($route = $request->attributes->get(RouteObjectInterface::ROUTE_OBJECT)) {
  $title = \Drupal::service('title_resolver')->getTitle($request, $route);
}
