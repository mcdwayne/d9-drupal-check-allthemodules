<div{{ attributes.addClass('result-item') }}>
    <h3>{{ label }}</h3>
    <div>{{ question_body }}</div>
    <div>{{ score }}</div>
    {% if content %}
        {{- content -}}
    {% endif %}
</div>
