deploy_key.regenerate_confirm:
  path: '/deploy-key/regenerate/{entity_type}/{id}'
  defaults:
    _title: 'Regenerate deploy key'
    _form: 'Drupal\deploy_key\Form\RegenerateConfirmForm'
  requirements:
    _permission: 'access content'
