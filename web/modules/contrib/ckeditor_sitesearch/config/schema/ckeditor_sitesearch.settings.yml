# Schema for the configuration files of the CKEditor Site Search module.

ckeditor.plugin.search:
  type: config_object
  label: 'CKEditor Search settings'
  mapping:
    enable:
      type: boolean
      label: 'Enabled'
    search_path:
      type: string
      label: 'Search Path'