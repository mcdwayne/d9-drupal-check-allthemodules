services:
  replicate_group.event_subscriber.group:
    class: \Drupal\replicate_group\EventSubscriber\ReplicateGroupSubscriber
    arguments:
      - '@group.membership_loader'
      - '@current_user'
      - '@plugin.manager.group_content_enabler'
    tags:
      - { name: event_subscriber }