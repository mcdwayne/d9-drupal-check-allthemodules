commands:
  drd:
    migrate:
      from:
        d7:
          description: 'Import an inventory JSON file from DRD 7.x and configure all included cores and domains'
          arguments:
            inventory: 'JSON file that contains the inventory as exported from DRD 7.x'
