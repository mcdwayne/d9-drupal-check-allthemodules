<?php

namespace Drupal\{{ machine_name }}\Commands;

use Drupal\drd\Commands\DrdCommands;

/**
 * Class {{ class }}.
 *
 * @package Drupal\drd
 */
class {{ class }}Commands extends DrdCommands {

  /**
   * TODO: Describe the action.
   *
   * @param array $options
   *   CLI options.
   *
   * @command drd:{{ machine_name }}:{{ id }}
   * @option tag Tag name
   * @option host Host name
   * @option host-id Host ID
   * @option core Core name
   * @option core-id Core ID
   * @option domain Domain name
   * @option domain-id Domain ID
   */
  public function {{ id }}(array $options = [
    'tag' => NULL,
    'host' => NULL,
    'host-id' => NULL,
    'core' => NULL,
    'core-id' => NULL,
    'domain' => NULL,
    'domain-id' => NULL,
  ]) {
    $this->actionKey = '{{ machine_name }}_drd_action_{{ id }}';
    $this->options = $options;
{% if drush_callback %}
    $this->entities = $this->service()->{{ drush_callback }}();
{% endif %}
    $this
      ->prepare()
      ->execute();
  }

}
