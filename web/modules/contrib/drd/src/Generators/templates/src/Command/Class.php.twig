<?php

namespace Drupal\{{ machine_name }}\Command;

use Drupal\drd\Command\{{ dc_base_class }};
use Drupal\drd\Plugin\Action\BaseInterface as ActionBaseInterface;
use Symfony\Component\Console\Input\InputInterface;

/**
 * Class {{ class }}.
 *
 * @package Drupal\drd
 */
class {{ class }} extends {{ dc_base_class }} {

  /**
   * Construct the {{ class }} command.
   */
  public function __construct() {
    parent::__construct();
    $this->actionKey = '{{ machine_name }}_drd_action_{{ id }}';
  }

  /**
   * {@inheritdoc}
   */
  protected function configure() {
    parent::configure();
    $this
      ->setName('drd:{{ machine_name }}:{{ id }}');
    // TODO: Add command configuration.
  }

  /**
   * {@inheritdoc}
   */
  protected function setActionArguments(ActionBaseInterface $action, InputInterface $input) {
    parent::setActionArguments($action, $input);
    // TODO: Set action arguments.
  }

}
