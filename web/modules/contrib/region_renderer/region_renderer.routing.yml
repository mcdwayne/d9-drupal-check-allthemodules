region_renderer.render.region:
  path: '/render/region/{region}'
  defaults:
    _controller: 'Drupal\region_renderer\Controller\RegionRendererController::renderRegion'
  requirements:
    _permission: 'access content'
