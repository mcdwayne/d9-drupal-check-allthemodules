id: gitlab_time_tracker_user
migration_tags:
  - users
migration_group: gitlab_time_tracker
label: 'User accounts'
source:
  plugin: gitlab_user
  track_changes: true
process:
  name: username
  mail: email
  status:
    plugin: static_map
    source: state
    map:
      blocked: 0
      active: 1
  field_gitlab_id: id
  preferred_langcode: "@langcode"
destination:
  plugin: 'entity:user'
migration_dependencies:
