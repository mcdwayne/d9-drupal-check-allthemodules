name: "OAuth 2.0"
description: "Drupal OAuth 2.0 Authorization Provider."
package: "OAuth 2.0"

type: module
core: 8.x

dependencies:
  - basic_auth
  - rest
  - system (>=8.4.0)
  - user

configure: oauth2.settings
