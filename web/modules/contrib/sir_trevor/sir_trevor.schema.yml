# Schema for Sir Trevor module's configuration objects.

sir_trevor.global:
  type: config_object
  label: 'Global Sir Trevor configuration'
  mapping:
    enabled_blocks:
      type: sequence
      label: 'Enabled blocks'
      translatable: false
