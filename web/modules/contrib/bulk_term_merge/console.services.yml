services:
  bulk_term_merge.default:
    class: Drupal\bulk_term_merge\Command\BulkTermMergeCommand
    arguments: ['@entity_type.manager', '@bulk_term_merge.duplicate_finder', '@term_merge.term_merger']
    tags:
      - { name: drupal.command }
