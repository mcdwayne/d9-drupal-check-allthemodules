<?php

/**
 * Implementation of hook_theme() function.
 * @param type $existing
 * @param type $type
 * @param type $theme
 * @param type $path
 * @return array
 */
function surveygizmodrupal_theme($existing, $type, $theme, $path) {
  return [
    'surveygizmo_listing_page' => [
      'variables' => ['data' => NULL],
      'template' => 'surverygizmo-listing-page',
    ],
    'surveygizmo_survey_form' => [
      'render element' => 'form',
      'template' => 'surverygizmo-detial-page',
    ],
  ];
}
