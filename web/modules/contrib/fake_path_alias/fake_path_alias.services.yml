services:
  fake_path_alias.path_processor_alias:
    class: Drupal\fake_path_alias\PathProcessorFakeAlias
    tags:
      - { name: path_processor_inbound, priority: 100 }
      - { name: path_processor_outbound, priority: 100 }
    arguments: ['@path.alias_manager', '@path.alias_storage', '@language_manager']
