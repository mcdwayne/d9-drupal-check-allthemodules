{#
/**
 * @file formazing_entity.html.twig
 * Default theme implementation to present Formazing entity data.
 *
 * This template is used when viewing Formazing entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_formazing_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('formazing_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
