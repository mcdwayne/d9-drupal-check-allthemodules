{#
/**
 * @file
 * Theme override for a vcard icon.
 *
 * Available variables:
 * - url: An internal system path or a fully qualified external URL of the vCard.
 * - attributes: Remaining HTML attributes for the vCard link.
 *   - title: A descriptive title of the feed link.
 *   - class: HTML classes to be applied to the feed link.
 */
#}
<a href="{{ url }}"{{ attributes.addClass('vcard-icon') }}>
    {{ 'Download vCard(s)'|t }}
</a>