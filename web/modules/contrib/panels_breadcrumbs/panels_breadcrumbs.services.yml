services:
  panels_breadcrumbs.breadcrumb:
    class: Drupal\panels_breadcrumbs\Breadcrumb\PanelsBreadcrumbBuilder
    arguments: ['@router.route_provider', '@token', '@access_manager', '@router', '@current_user']
    tags:
      - { name: breadcrumb_builder, priority: -1 }
