services:
  logger.channel.druminate_sso:
    parent: logger.channel_base
    arguments: ['druminate_sso']
  pre_login.subscriber:
    class: Drupal\druminate_sso\Subscriber\DruminateSsoPreLoginSubscriber
    arguments: ['@config.factory', '@plugin.manager.druminate_endpoint']
    tags:
      - { name: 'event_subscriber' }
