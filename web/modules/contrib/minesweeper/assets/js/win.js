/**
 * @file
 * Defines the behavior of the Minesweeper gameboard.
 */

(function ($, Drupal, drupalSettings) {
  Drupal.behaviors.minesweeperGameBehaviorWin = {
    attach: function (context) {

    }
  };
})(jQuery, Drupal, drupalSettings);