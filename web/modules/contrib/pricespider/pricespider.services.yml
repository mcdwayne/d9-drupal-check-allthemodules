services:
  # Services.
  pricespider:
    class: Drupal\pricespider\Service\PriceSpiderService
    arguments: ['@config.factory', '@entity_type.manager', '@entity_field.manager', '@language_manager', '@module_handler', '@theme.manager', '@logger.factory']
