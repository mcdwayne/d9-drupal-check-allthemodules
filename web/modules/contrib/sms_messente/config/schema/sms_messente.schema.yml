sms_gateway.settings.messente:
  type: sms_gateway_settings
  mapping:
    host:
      type: string
      label: 'Messente host'
    secure:
      type: boolean
      label: 'Send messages over secure connection'
    user:
      type: string
      label: 'API Username'
    pass:
      type: string
      label: 'API Password'
    dlr:
      type: boolean
      label: 'Send delivery report URL for asyncronous delivery reporting with every message'
    screen:
      type: boolean
      label: 'Send straight to screen messages (Flash messages)'