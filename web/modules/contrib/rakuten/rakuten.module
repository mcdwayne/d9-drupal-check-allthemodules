<?php

/**
 * @file
 * Integrates Rakuten Web Service SDK for PHP.
 */

/**
 * Determine if rakuten is used or not.
 */
function rakuten_active() {
  // We need to rely on our 'rakuten_on' variable.
  return (bool) \Drupal::config('rakuten.settings')->get('rakuten_on');
}
