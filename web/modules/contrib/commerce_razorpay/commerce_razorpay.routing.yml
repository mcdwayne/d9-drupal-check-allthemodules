commerce_razorpay.capture_payment:
  path: '/capture-payment'
  defaults:
    _controller: '\Drupal\commerce_razorpay\Controller\Controller::capturePayment'
  requirements:
    _access: 'TRUE'
      #@TODO to be changed later.
      # Needs to be accessed by an anonymous user.
