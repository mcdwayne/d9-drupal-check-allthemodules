add_more_alternate.settings:
  type: config_object
  label: 'Add more alternate settings'
  mapping:
    add_item_label:
      type: string
      label: 'Add item label'
    remove_item_label:
      type: string
      label: 'Remove item label'
    add_item_classes:
      type: string
      label: 'Add item classes'
    remove_item_classes:
      type: string
      label: 'Remove item classes'
    admin_disable:
      type: integer
      label: 'Admin disable'
