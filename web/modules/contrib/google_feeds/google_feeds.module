<?php

/**
 * @file
 * Google Feeds Views module help and theme functions.
 */

\Drupal::moduleHandler()->loadInclude('google_feeds', 'inc', 'google_feeds.theme');

/**
 * Implements hook_theme().
 */
function google_feeds_theme($existing, $type, $theme, $path) {
  return [
    'google_feeds' => [
      'file' => 'google_feeds.theme.inc',
    ],
  ];
}
