<?php

/**
 * @file
 * Contains message close functionality.
 */

/**
 * Implements hook_page_attachments().
 *
 * For adding js and css files.
 */
function messageclose_page_attachments(array &$page) {
  $page['#attached']['library'][] = 'messageclose/messageclose';
}
