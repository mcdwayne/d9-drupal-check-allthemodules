role_paywall.settings:
  type: config_object
  label: "Access control settings."
  mapping:
    bundles:
      type: sequence
      label: "Bundles behind the paywall."
      sequence:
        type: string
    roles:
      type: sequence
      label: "Roles with access to all content"
      sequence:
        type: string
    activate_paywall_field:
      type: sequence
      label: "Fields to mark if content is premium."
      sequence:
        type: string
    hidden_fields:
      type: sequence
      label: "Hidden fields."
      sequence:
        type: sequence
        sequence:
          type: string
    barrier_block:
      type: string
      label: "Block id for login barrier"
