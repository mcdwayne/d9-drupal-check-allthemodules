services:
  redmine_connector.connect:
    class: Drupal\redmine_connector\ConnectService
    arguments: ['@current_user', '@request_stack', '@entity_type.manager',  '@config.factory', '@http_client', '@logger.factory']
  redmine_connector.grouping:
      class: Drupal\redmine_connector\GroupingService
  redmine_connector.synchronization:
      class: Drupal\redmine_connector\SynchronizationService
