parameters:
  pending_user_notification.service.class: Drupal\pending_user_notification\Service\PendingUserNotificationService

services:
  pending_user_notification.service:
    class: '%pending_user_notification.service.class%'
    arguments:
      - '@database'
