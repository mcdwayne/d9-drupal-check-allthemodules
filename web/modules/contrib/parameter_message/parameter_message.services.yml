services:
  parameter_message.event_subscriber:
    class: Drupal\parameter_message\ParameterMessageSubscriber
    arguments: ['@request_stack', '@config.factory', '@entity.query', '@entity.manager']
    tags:
      - { name: event_subscriber }
