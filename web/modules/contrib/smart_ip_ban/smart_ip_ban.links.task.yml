#give it the same name as the route to make it clear
smart_ip_ban.admin-ban:
  title: BLOCK IP ADDRESS
  route_name: ban.admin_page #route defined in .routing.yml
  base_route: system.admin_config_people #same as the route name
  weight: 0

#add another tab next to the home tab
smart_ip_ban.settings-ban: #name it same as route to avoid confusion
  title: SMART IP BAN SETTINGS
  #route defined in .routing.yml
  route_name: smart_ip_ban.settings #link to tab1 route
  #route defined in .routing.yml
  base_route: system.admin_config_people #set the base tab
  weight: 1

