services:
  # Experian validation service.
  experian_validation.service:
    class: Drupal\experian_validation\Services\ExperianValidationService
    arguments: ['@state', '@http_client', '@logger.factory']
