name: 'Demo Content Example'
description: 'Example module for demo content.'
type: module
core: 8.x
version: 8.x-1.0-dev
dependencies:
  - demo_content
demo_content:
  # The article nodes depends on the images, so we import file first.
  - content/file.file.yml

  # Next import the node articles.
  - content/node.article.yml
