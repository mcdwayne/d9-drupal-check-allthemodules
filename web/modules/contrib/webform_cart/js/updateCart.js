(function ($, Drupal) {
  Drupal.behaviors.updateCart = {
    attach: function (context, settings) {
      // $('.delete-item', context).once('search').bind('click', function() {
        // console.log('webform_cart');
        // $.ajax({url: "/webform_epetition/searchForRepresentatives/"+ postCode +"/"+ dataType +"/details", success: function(result){
        //     $('#results_rep').html(result);
        //   }});
      // });
    }
  };
})(jQuery, Drupal);
