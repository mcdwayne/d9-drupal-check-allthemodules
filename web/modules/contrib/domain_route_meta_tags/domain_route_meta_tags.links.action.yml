# All action links for this module

domain_route_meta_tags.meta_add:
  # Which route will be called by the link
  route_name: domain_route_meta_tags.meta_add
  title: 'Add Meta Tags'

  # Where will the link appear, defined by route name.
  appears_on:
    - entity.domain_route_meta_tags.collection
    - entity.domain_route_meta_tags.canonical
entity.domain_route_meta_tags.collection:
  # Which route will be called by the link
  route_name: entity.domain_route_meta_tags.collection
  title: 'List Meta Tags'

  # Where will the link appear, defined by route name.
  appears_on:
    - domain_route_meta_tags.meta_add
