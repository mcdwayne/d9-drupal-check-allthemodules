<?php

/**
 * @file
 * Defines common functionality for all Drupaneo modules.
 */

/**
 * Implements hook_toolbar().
 */
function drupaneo_toolbar() {
    return array(
        'drupaneo' => array(
            '#type' => 'toolbar_item',
            '#attached' => array('library' => array('drupaneo/toolbar'))
        )
    );
}
