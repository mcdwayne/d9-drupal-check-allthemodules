name: Moderation Scheduler
type: module
description: 'Moderation Scheduler is module to schedule content in moderation state to be published at certain date and time running cron job.'
package: Schedule content moderation
configure: moderation_scheduler.settings
# core: 8.x
# core: 8.x
# version: VERSION

dependencies:
  - drupal:datetime
  - drupal:field
  - drupal:node
  - drupal:views

# Information added by Drupal.org packaging script on 2019-03-24
version: '8.x-5.8'
core: '8.x'
project: 'moderation_scheduler'
datestamp: 1553471887
