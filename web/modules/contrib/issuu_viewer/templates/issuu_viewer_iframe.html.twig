{#
/**
 * @file
 * Iframe of the issuu viewer document.
 *
 * Available variables:
 *   - document_id: document id of the issuu viewer.
 *   - height: the height of the iframe.
 *   - background_color: the background of the iframe.
 */
#}
<iframe width="100%" height="{{ height }}"
        src="//e.issuu.com/legacy.html?documentId={{ document_id }}&
 backgroundColor={{ background_color }}" frameborder="0" allowfullscreen>
</iframe>