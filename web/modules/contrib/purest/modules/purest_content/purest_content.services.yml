services:
  # Simplifies and standardises the response of node and taxonomy 
  # term entities
  purest.content:
    class: Drupal\purest_content\Normalizer\ContentNormalizer
    arguments: ['@entity.manager','@entity_type.repository','@config.factory','@entity_type.bundle.info','@entity_field.manager']
    tags:
      - { name: normalizer, priority: 35 }
