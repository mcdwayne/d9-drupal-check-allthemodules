services:
  default_logger:
    class: Drupal\file_ownage\Logger\DefaultLogger
    tags:
      -
        name: logger

  file_ownage.find_manager:
    class: Drupal\file_ownage\FindManager
    arguments: ['@config.factory', '@http_client', '@file_system']
