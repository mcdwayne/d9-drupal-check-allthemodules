services:
  prometheus_exporter.commands:
    class: \Drupal\prometheus_exporter\Commands\PrometheusExporterCommands
    arguments: ['@prometheus_exporter.metrics_collector_manager', '@prometheus_exporter.serializer']
    tags:
      - { name: drush.command }
