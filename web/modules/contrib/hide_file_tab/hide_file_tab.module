<?php

/**
 * @file
 * Hides file tab in admin.
 */

/**
 * Implements hook_page_attachments().
 */
function hide_file_tab_page_attachments(array &$page) {
  if (\Drupal::currentUser()->isAnonymous()) {
    return;
  }

  $page['#attached']['library'][] = 'hide_file_tab/drupal.hide-file-tab';
}
