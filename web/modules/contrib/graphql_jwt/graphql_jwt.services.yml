services:
  graphql_jwt.config:
    class: Drupal\Core\Config\ImmutableConfig
    factory: config.factory:get
    arguments:
      - graphql_jwt.config

  logger.channel.graphql_jwt:
    class: Drupal\Core\Logger\LoggerChannel
    factory: logger.factory:get
    arguments: ['graphql_jwt']
