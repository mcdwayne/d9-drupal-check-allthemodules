# Schema for the configuration files of the JWT module.

graphql_jwt.config:
  type: config_object
  label: 'GraphQL JWT settings'
  mapping:
    session_destroy:
      type: boolean
      label: 'Destroys the session on successful authentication.'
