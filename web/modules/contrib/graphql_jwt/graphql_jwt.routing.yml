graphql_jwt.graphql_jwt_config_form:
  path: '/admin/config/system/graphql_jwt'
  defaults:
    _form: '\Drupal\graphql_jwt\Form\ConfigForm'
    _title: 'GraphQL JWT Configuration'
  requirements:
    _permission: 'access administration pages'
  options:
    _admin_route: TRUE
