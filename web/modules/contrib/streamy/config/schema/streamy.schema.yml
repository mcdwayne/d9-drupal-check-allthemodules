streamy.local:
  type: config_object
  mapping:
    plugin_configuration:
      type: sequence
      label: 'The plugin configuration'
      sequence:
        type: sequence
        label: 'Plugin sub-key configuration'
        sequence:
          type: sequence
          label: 'Plugin sub-key configuration'
          sequence:
            type: string
            label: 'Plugin sub-key configuration'

streamy.schemes:
  type: config_object
  mapping:
    schemes:
      type: mapping
      label: 'Defines a Streamy scheme'
      mapping:
        streamy:
          type: mapping
          label: 'Stream information'
          mapping:
            name:
              type: string
              label: 'Stream name'
            description:
              type: string
              label: 'Stream description'
            private:
              type: boolean
              label: 'Stream access'
        streamypvt:
          type: mapping
          label: 'Stream information'
          mapping:
            name:
              type: string
              label: 'Stream name'
            description:
              type: string
              label: 'Stream description'
            private:
              type: boolean
              label: 'Stream access'

streamy.streamy:
  type: config_object
  mapping:
    plugin_configuration:
      type: sequence
      label: 'The plugin configuration'
      sequence:
        type: sequence
        label: 'Plugin sub-key configuration'
        sequence:
          type: string
          label: 'Plugin sub-key configuration'