services:
  streamy_aws_uninstall_validator:
    class: Drupal\streamy_aws\StreamyAwsUninstallValidator
    tags:
      - { name: module_install.uninstall_validator }
    arguments: ['@config.factory']
    lazy: true