streamy_aws.awsv3:
  type: config_object
  mapping:
    plugin_configuration:
      type: sequence
      label: 'The plugin configuration'
      sequence:
        type: sequence
        label: 'Plugin sub-key configuration'
        sequence:
          type: sequence
          label: 'Plugin sub-key configuration'
          sequence:
            type: string
            label: 'Plugin sub-key configuration'

streamy_aws.awscdn:
  type: config_object
  mapping:
    plugin_configuration:
      type: sequence
      label: 'The plugin configuration'
      sequence:
        type: sequence
        label: 'Plugin sub-key configuration'
        sequence:
          type: string
          label: 'Plugin sub-key configuration'
