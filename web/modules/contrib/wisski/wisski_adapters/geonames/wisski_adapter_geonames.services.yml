services:
  cache.wisski_adapter_geonames:
    class: Drupal\Core\Cache\CacheBackendInterface
    tags:
      - { name: cache.bin }
    factory: cache_factory:get
    arguments: [wisski_adapter_geonames]
