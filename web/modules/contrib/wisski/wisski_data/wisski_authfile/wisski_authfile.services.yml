services:
  cache.wisski_authfile_lod_engine:
    class: Drupal\Core\Cache\CacheBackendInterface
    tags:
      - { name: cache.bin }
    factory: cache_factory:get
    arguments: [wisski_authfile_lod_engine]

