loading_bar.settings:
  path: /admin/config/content/loading-bar
  defaults:
    _form: \Drupal\loading_bar\Form\LoadingBarSettingsForm
    _title: Loading bar settings
  requirements:
    _permission: access administration pages
