<?php

/**
 * Implements hook_uninstall().
 */
function demo_uninstall() {
  \Drupal::service('config.factory')->getEditable('demo.settings')->clear('demo_reset_last')->save();
  \Drupal::service('config.factory')->getEditable('demo.settings')->clear('demo_dump_cron')->save();
}
