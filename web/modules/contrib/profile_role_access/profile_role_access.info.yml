name: Profile Role Visibility
type: module
description: Restrict access to certain user profiles based on role.
core: 8.x
configure: profile_role_access.profile_role_access_settings
dependencies:
  - rules
