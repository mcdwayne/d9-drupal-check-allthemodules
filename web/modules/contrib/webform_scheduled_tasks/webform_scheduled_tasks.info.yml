name: Webform Scheduled Tasks
description: Schedule tasks to run on groups of webform submissions.
type: module
core: 8.x
dependencies:
  - webform:webform
  - drupal:file
test_dependencies:
  - healthz:healthz
