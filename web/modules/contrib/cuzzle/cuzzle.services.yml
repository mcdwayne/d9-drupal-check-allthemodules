services:
  cuzzle.middleware:
    class: Drupal\cuzzle\MiddlewareWrapper
    arguments: ['@logger.channel.cuzzle']
    tags:
      - { name: http_client_middleware }

  logger.channel.cuzzle:
    parent: logger.channel_base
    arguments: ['cuzzle']
