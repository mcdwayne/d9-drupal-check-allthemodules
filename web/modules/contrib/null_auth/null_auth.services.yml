services:
  authentication.null_auth:
    class: Drupal\null_auth\Authentication\Provider\NullAuth
    arguments: ['@entity_type.manager']
    tags:
      - { name: authentication_provider, provider_id: null_auth, priority: 100 }
