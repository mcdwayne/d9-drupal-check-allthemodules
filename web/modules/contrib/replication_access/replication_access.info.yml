name: 'Replication Access Filter'
type: module
description: 'Provides ability to replicate only documents for a particular user'
# version: VERSION
# core: 8.x
configure: replication_access.settings
dependencies:
  - replication

# Information added by Drupal.org packaging script on 2017-11-01
core: '8.x'

