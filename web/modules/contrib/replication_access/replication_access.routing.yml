# Remote routing definition
replication_access.settings:
  path: '/admin/config/replication/access'
  defaults:
    _form: '\Drupal\replication_access\Form\SettingsForm'
    _title: 'Replication Access settings'
  requirements:
    _permission: 'access administration pages'
  options:
    _admin_route: TRUE
