drush_task.config:
  path: '/admin/config/services/drush_task'
  defaults:
    _title: 'Manage drush settings'
    _form: 'Drupal\drush_task\Form\ConfigForm'
  requirements:
    _permission: 'administer site configuration'
