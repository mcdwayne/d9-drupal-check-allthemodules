# Declares that our drush task object is a service that can be invoked.
services:
  drush_task.drush_task:
    class: Drupal\drush_task\DrushTask
    # Tells the caller to pass in a configuration handle to the constructor.
    arguments: ['@config.factory']
