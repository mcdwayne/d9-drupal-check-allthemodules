# page.
id: store_page
label: Store page
migration_group: store
dependencies:
  enforced:
    module:
      - cmlstarter_demo
source:
  plugin: s_page
destination:
  plugin: entity:node
process:
  uid: uid
  type: type
  title: title
  status: status
  sticky: sticky
  changed: changed
  body: body
