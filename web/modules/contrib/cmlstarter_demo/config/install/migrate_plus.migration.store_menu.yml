# Menu.
id: store_menu
label: Store-Menu
migration_group: store
dependencies:
  enforced:
    module:
      - cmlstarter_demo
migration_dependencies:
  required:
    - store_page
source:
  plugin: s_menu
destination:
  plugin: entity:menu_link_content
process:
  bundle: bundle
  menu_name: menu
  title: title
  link/uri: uri
  external: external
  weight: weight
  expanded: expanded
  enabled: enabled
  parent: parent
  changed: changed
