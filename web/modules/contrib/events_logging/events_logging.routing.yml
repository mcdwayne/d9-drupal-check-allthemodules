events_logging.config_form:
  path: '/admin/config/events_logging'
  defaults:
    _form: '\Drupal\events_logging\Form\ConfigForm'
    _title: 'Events Logging Configuration'
  requirements:
    _permission: 'access administration pages'
  options:
    _admin_route: TRUE

