{#
/**
 * @file webpay_transaction.html.twig
 * Default theme implementation to present Webpay transaction data.
 *
 * This template is used when viewing Webpay transaction pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_webpay_transaction()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('webpay_transaction') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
