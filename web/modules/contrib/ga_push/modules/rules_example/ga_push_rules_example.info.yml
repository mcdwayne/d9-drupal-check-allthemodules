name: Google Analytics Rules Example
description:  Google Analytics module that pushes events when a comment is made (example module).
package: Statistics
type: module
core: 8.x

dependencies:
  - ga_push:ga_push
  - rules:rules
