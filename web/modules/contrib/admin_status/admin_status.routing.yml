# This defines the config form page's path to the routing system.
admin_status.admin_status_form:
  path: '/admin/config/system/admin_status'
  defaults:
    _title: 'Admin Status'
    _form: '\Drupal\admin_status\Form\AdminStatusForm'
  requirements:
    _permission: 'administer admin status'
