services:
  omeda:
    class: Drupal\omeda\Omeda
    arguments: ['@config.factory', '@encryption', '@logger.channel.omeda', '@string_translation']
  logger.channel.omeda:
    parent: logger.channel_base
    arguments: ['omeda']
