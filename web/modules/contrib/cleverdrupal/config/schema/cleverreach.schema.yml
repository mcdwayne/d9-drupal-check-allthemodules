# Schema for the configuration files of the CleverReach module.
cleverreach.settings:
  type: config_object
  label: 'CleverReach® Settings'
  mapping:
    installed:
      type: boolean
      label: 'INSTALLED'
    min_log_level:
      type: string
      label: 'MIN_LOG_LEVEL'
    access_token:
      type: string
      label: 'ACCESS_TOKEN'
    integration_id:
      type: string
      label: 'INTEGRATION_ID'
    default_logger_status:
      type: boolean
      label: 'DEFAULT_LOGGER_STATUS'
    max_started_task_limit:
      type: string
      label: 'MAX_STARTED_TASK_LIMIT'
    first_email_built:
      type: boolean
      label: 'FIRST_EMAIL_BUILT'
    task_runner_wakeup_delay:
      type: integer
      label: 'TASK_RUNNER_WAKEUP_DELAY'
    max_alive_time:
      type: integer
      label: 'MAX_ALIVE_TIME'
    max_task_execution_retries:
      type: integer
      label: 'MAX_TASK_EXECUTION_RETRIES'
    max_task_inactivity_period:
      type: integer
      label: 'MAX_TASK_INACTIVITY_PERIOD'
    max_task_inactivity_period:
      type: integer
      label: 'MAX_TASK_INACTIVITY_PERIOD'
    task_runner_status:
      type: mapping
      label: 'TASK_RUNNER_STATUS'
      mapping:
        guid:
          type: string
          label: 'Guid'
        timestamp:
          type: integer
          label: 'Timestamp'
    user_info:
      type: mapping
      label: 'CLEVERREACH_USER_INFO'
      mapping:
        name:
          type: id
          label: 'Id'
        name:
          type: string
          label: 'Name'
        email:
          type: string
          label: 'Email'
    article_search_password:
      type: string
      label: 'ARTICLE_SEARCH_PASSWORD'
    recipient_sync_batch_size:
      type: integer
      label: 'RECIPIENT_SYNC_BATCH_SIZE'
