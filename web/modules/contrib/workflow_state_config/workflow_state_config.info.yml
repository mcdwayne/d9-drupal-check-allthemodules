name: Workflow State Config
description: 'Workflow State Configuration Set Status and Default Revision For Node. On which state that node should get "Published" and make it as "Default Revision".'
type: module
core: 8.x
package: Workflow
dependencies:
  - workflow
