<?php

/**
 * @file
 * Contains Drupal\{{ module_name }}\Entity\{{ entity_class }}.
 */

namespace Drupal\{{ module_name }}\Entity;

use Drupal\Core\Config\Entity\ConfigEntityBase;
use Drupal\{{ module_name }}\{{ entity_class }}Interface;

/**
 * Defines the Example entity.
 *
 * @ConfigEntityType(
 *   id = "{{ entity_name }}",
 *   label = @Translation("{{ entity_class }}"),
 *   handlers = {
 *     "list_builder" = "Drupal\{{ module_name }}\Controller\{{ entity_class }}ListBuilder",
 *     "form" = {
 *       "add" = "Drupal\{{ module_name }}\Form\{{ entity_class }}Form",
 *       "edit" = "Drupal\{{ module_name }}\Form\{{ entity_class }}Form",
 *       "delete" = "Drupal\{{ module_name }}\Form\{{ entity_class }}DeleteForm"
 *     }
 *   },
 *   config_prefix = "{{ entity_name }}",
 *   admin_permission = "administer site configuration",
 *   entity_keys = {
 *     "id" = "id",
 *     "label" = "label",
 *     "uuid" = "uuid"
 *   },
 *   links = {
 *     "edit-form" = "/admin/config/system/{{ entity_name }}/{{ '{'~entity_name~'}' }}",
 *     "delete-form" = "/admin/config/system/{{ entity_name }}/{{  '{'~entity_name~'}' }}/delete",
 *     "collection" = "/admin/config/system/{{ entity_name }}"
 *   }
 * )
 */
class {{ entity_class }} extends ConfigEntityBase implements {{ entity_class }}Interface {
  /**
   * The {{ entity_class }} ID.
   *
   * @var string
   */
  protected $id;

  /**
   * The {{ entity_class }} label.
   *
   * @var string
   */
  protected $label;

}
