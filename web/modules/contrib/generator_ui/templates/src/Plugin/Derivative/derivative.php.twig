<?php
/**
 * @file
 * Contains \Drupal\{{ module_name }}\Plugin\Derivative\{{ class_derivative }}.
 */

namespace Drupal\{{ module_name }}\Plugin\Derivative;

use Drupal\Component\Plugin\Derivative\DeriverBase;
/**
 * Class {{ class_derivative }}
 * @package Drupal\{{ module_name }}\Plugin\Derivative
 */
class {{ class_derivative }} extends DeriverBase {


  /**
   * {@inheritdoc}
   */
  public function getDerivativeDefinitions($base_plugin_definition) {
    //This is an example how to create two blocks
    $myBlocks = array(
      'first_block' => t('My first Block'),
      'second_block' => t('My Second Block')
    );
    foreach($myBlocks as $block_id => $block_label){
      $this->derivatives[$block_id] = $base_plugin_definition;
      $this->derivatives[$block_id]['admin_label'] = $block_label;
      $this->derivatives[$block_id]['cache'] = DRUPAL_NO_CACHE;
    }
    return $this->derivatives;
  }

}