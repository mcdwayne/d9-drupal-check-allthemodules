<?php
{%  if create_entity %}
{% if help %}
//Creating a new entity object.
//The type of the entity : node
// An array of values to set, keyed by property name : in this example we have 2 params :'type' => 'page' and 'title' => 'Hello World page' )
{% endif %}
{% if contents != "" %}
$new_page = entity_create('{{ entity_type_create }}', array('type' => '{{ contents }}', 'title' => 'Hello World page'));
{% else %}
$new_page = entity_create('{{ entity_type_create }}', array();
{% endif %}
{% if help %}
//Another method calling Global class "Drupal"
//$new_page = \Drupal::entityManager()->getStorage('node')->create(array('type' => 'page', 'title' => 'Hello World page'));
{% endif %}
$new_page->save();
{% endif %}
{% if modify_entity %}
//Load the entity "{{ entity_type_modify }}" which have an id = 1.
//${{ entity_type_modify }} = entity_load('{{ entity_type_modify }}',1);
//Example : Set the title of the entity "node" which have an id = 1.
//$node->set('title', 'Another title');
{% endif %}
{% if delete_entity %}
//Deletes multiple entities permanently.
{% if help %}
// 2 params :
//- The type of the entity: {{ entity_type_delete }} in this example.
//-  An array of entity IDs of the entities to delete. : 1,2 in this example
{% endif %}
//entity_delete_multiple('{{ entity_type_delete }}',array(1,2));
{% endif %}