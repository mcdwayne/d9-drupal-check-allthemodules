services:
  {{ service_name }}:
    class: Drupal\{{ module_name }}\EventSubscriber\{{ file_name }}
    arguments: [{% for container in containers if containers and containers|length>0 %}"@{{ container['name'] }}"{% if loop.index< containers|length %}, {% endif %}{% endfor %}]
    tags:
      - { name: event_subscriber }