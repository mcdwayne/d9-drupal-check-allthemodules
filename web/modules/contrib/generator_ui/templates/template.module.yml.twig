<?php

/**
 * @file
 * Contains {{ module_name }}.module.
 */

/**
 *  Implements hook_theme().
 */
function {{ module_name }}_theme() {
{% if help %}
  // Create a template named contact_template
  // Initialising variables
  // variable attributes responsable for add ids and classes
  // we initialising also four variables which can be printed in the controller.
{% endif %}
  return array(
    'contact_template' => array(
      'variables' => array(
        'attributes' => array(),
        'name' => NULL,
        'surname' => NULL,
        'address' => NULL,
        'number' => NULL,
      ),
    ),
  );
}