<?php
 //Attach the library to a render array in a hook.
 /**
  *  Implements hook_preprocess().
  */
 function {{ module_name }}_preprocess(&$variables, $hook) {
   $variables['#attached']['library'][] = '{{ module_name }}/{{ file_name }}';
 }
 