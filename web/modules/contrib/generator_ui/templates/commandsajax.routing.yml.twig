{{ module_name }}.ajaxcommands:
  path: '/ajax-commands'
  defaults:
    _form: '\Drupal\{{ module_name }}\Form\{{ form }}'
    _title: 'Generate Commands Ajax in D8'
  requirements:
    _permission: 'access content'