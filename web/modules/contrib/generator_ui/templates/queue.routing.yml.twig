{{ module_name }}.{{ id_queue }}:
  path: '/publish-node-queue'
  defaults:
    _controller: '\Drupal\{{ module_name }}\Controller\{{ controller_class }}::publishNodeWithQueue'
  requirements:
    _permission: 'access administration pages'
