{{ module_name }}.autocomplete:
  path: '/autocomplete'
  defaults:
    _controller: '\Drupal\{{ module_name }}\Controller\{{ controller }}::autocomplete'
    _title: 'Autocomplete'
  requirements:
    _format: 'json'
    _permission: 'access content'

{{ module_name }}.example:
  path: '/autocompleate-example'
  defaults:
    _form: '\Drupal\{{ module_name }}\Form\{{ form }}'
    _title: 'Generate example of autocompleate'
  requirements:
    _permission: 'access content'