{% if help %}
# Default values for the parameters (machine name and default value). One per line.
{% endif %}
email: 'user@example.fr'
{% if help %}
# For arrays of parameter, put the key and then indent all elements with key: 'value'.
{% endif %}
contact:
  name: 'My Name by default'
  surname: 'My Surname by default'