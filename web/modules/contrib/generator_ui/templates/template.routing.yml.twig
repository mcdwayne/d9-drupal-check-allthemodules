{{ module_name }}.profile:
  path: '/profile'
  defaults:
    _controller: '\Drupal\{{ module_name }}\Controller\{{ controller }}::build'
    _title: 'Profile page'
  requirements:
    _permission: 'access content'
