{{ module_name }}.batch_example:
  path: '/batch-example'
  defaults:
    _controller: '\Drupal\{{ module_name }}\Controller\{{ controller }}::publishAndUnpublishNodeBatch'
    _title: 'Example Publish and unpublish nodes via atch'
  requirements:
    _permission: 'access content'
