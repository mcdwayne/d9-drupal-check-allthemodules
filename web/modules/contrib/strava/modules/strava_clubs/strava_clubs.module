<?php

/**
 * Implements hook_theme().
 */
function strava_clubs_theme($existing, $type, $theme, $path) {
  return [
    'club' => [
      'render element' => 'elements',
      'template' => 'club',
      'file' => 'club.page.inc',
    ],
  ];
}
