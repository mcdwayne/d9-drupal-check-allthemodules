<?php

/**
 * @file
 * Installation functions for NASA module.
 */

use Drupal\user\RoleInterface;

/**
 * Implements hook_install().
 */
function nasa_install() {
  user_role_change_permissions(RoleInterface::ANONYMOUS_ID, array(
    'access apod' => TRUE,
  ));
}