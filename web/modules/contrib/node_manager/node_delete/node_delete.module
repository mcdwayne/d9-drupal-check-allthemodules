<?php

use Drupal\Core\Form\FormStateInterface;

/**
 * Implements hook_cron().
 */
function node_delete_cron() {

  $delete = \Drupal::config('node_delete.settings')->get('enable');

  if($delete) {
    // Delete node.
    \Drupal::service('node_delete.delete')->deleteNode();
  }
}