services:
  google_crawl_errors.commands:
    class: Drupal\google_crawl_errors\Commands\GoogleCrawlErrorsCommands
    arguments: ['siteId', 'siteUrl', 'category', 'platform']
    tags:
      - { name: drush.command }
