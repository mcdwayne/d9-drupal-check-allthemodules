dpl.preview:
  path: /node/{node}/dpl/{decoupled_preview_link}/preview
  defaults:
    _controller: \Drupal\dpl\Controller::preview
  options:
    _node_operation_route: TRUE
  requirements:
    _permission: "preview decoupled link"
    _csrf_token: 'TRUE'
    
