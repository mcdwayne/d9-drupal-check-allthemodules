image_edit.replace_image:
  path: 'xi/replace/{fileId}'
  defaults:
    _controller: 'Drupal\image_edit\Controller\ReplaceImage::replaceImage'
  methods:  [POST]
  requirements:
    _permission: 'use image edit'
  options:
      no_cache: 'TRUE'