# Schema for the configuration files of the File module.

wodby.settings:
  type: mapping
  label: 'Wodby settings'
  mapping:
    app_uuid:
      type: string
      label: 'Self App UUID'
    instances:
      type: mapping
      mapping:
        quick_deploy:
          type: sequence
          label: 'Quick Deploy instances list UUID'
