<?php

/**
 * @file
 * Contains eid_auth.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;
use Drupal\Core\Entity\EntityTypeInterface;

/**
 * Implements hook_help().
 */
function eid_auth_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the eid module.
    case 'help.page.eid_auth':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('ID card and mobile ID login') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function eid_auth_theme($existing, $type, $theme, $path) {
  $theme = [];

  $theme['eid_auth_login_content'] = [
    'variables' => [
      'forms' => NULL,
      'enabled_methods' => NULL,
    ],
    'template' => 'eid-auth-login-content',
  ];

  $theme['eid_auth_login_form'] = [
    'render element' => 'form',
    'template' => 'eid-auth-login-form',
  ];

  return $theme;
}

/**
 * Implements hook_entity_bundle_field_info_alter().
 */
function eid_auth_entity_bundle_field_info_alter(&$fields, EntityTypeInterface $entity_type, $bundle) {
  if ($bundle == 'user') {
    if (isset($fields['field_personal_id_code'])) {
      /* @var $fields \Drupal\field\Entity\FieldConfig[] */
      $fields['field_personal_id_code']->addConstraint('UniqueField', ['message' => t('User with same personal id code already exists!')]);
    }
  }
}

/**
 * Implements hook_preprocess_HOOK().
 */
function eid_auth_preprocess_eid_auth_login_content(&$variables) {
  $variables['module_path'] = base_path() . drupal_get_path('module', 'eid_auth');
}
