<?php

/**
 * @file
 * Uninstall functions for the max_cdn_cache module.
 */

/**
 * Implements hook_uninstall().
 */

function max_cdn_cache_uninstall() {
  // Deleting the validation settings while uninstalling the module.
  \Drupal::configFactory()->getEditable('max_cdn_cache_alias')->delete();
  \Drupal::configFactory()->getEditable('max_cdn_cache_consumer_key')->delete();
  \Drupal::configFactory()->getEditable('max_cdn_cache_consumer_secret')->delete();
}
