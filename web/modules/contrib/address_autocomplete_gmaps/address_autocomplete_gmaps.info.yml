name: 'Address autocomplete (with Google Maps API)'
type: module
description: 'Provides an autocomplete field for the address module through the Google Maps Javascript API. (Requires key)'
core: 8.x
package: 'Field types'
dependencies:
  - address
configure: address_autocomplete_gmaps.configure
