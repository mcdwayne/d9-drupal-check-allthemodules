services:
  update_runner.update_runner_process:
    class: Drupal\update_runner\Command\UpdateRunnerProcessorCommand
    arguments: []
    tags:
      - { name: drupal.command }
  update_runner.update_runner_fetch:
    class: Drupal\update_runner\Command\UpdateRunnerFetcherCommand
    arguments: []
    tags:
      - { name: drupal.command }
