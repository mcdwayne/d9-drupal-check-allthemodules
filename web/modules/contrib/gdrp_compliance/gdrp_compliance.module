<?php

/**
 * @file
 * Contains Hooks.
 */

use Drupal\gdrp_compliance\Hook\PageBottom;
use Drupal\gdrp_compliance\Hook\FormNodeFormAlter;
use Drupal\gdrp_compliance\Hook\FormContactMessageFormAlter;
use Drupal\gdrp_compliance\Hook\FormUserRegisterFormAlter;
use Drupal\gdrp_compliance\Hook\FormUserLoginFormAlter;

/**
 * Implements hook_page_bottom().
 */
function gdrp_compliance_page_bottom(array &$page_bottom) {
  PageBottom::hook($page_bottom);
}

/**
 * Implements hook_form_TYPE_alter().
 */
function gdrp_compliance_form_node_form_alter(&$form, $form_state, $form_id) {
  FormNodeFormAlter::hook($form, $form_state, $form_id);
}

/**
 * Implements hook_form_TYPE_alter().
 */
function gdrp_compliance_form_contact_message_form_alter(&$form, $form_state, $form_id) {
  FormContactMessageFormAlter::hook($form, $form_state, $form_id);
}

/**
 * Implements hook_form_TYPE_alter().
 */
function gdrp_compliance_form_user_register_form_alter(&$form, $form_state, $form_id) {
  FormUserRegisterFormAlter::hook($form, $form_state, $form_id);
}

/**
 * Implements hook_form_TYPE_alter().
 */
function gdrp_compliance_form_user_login_form_alter(&$form, $form_state, $form_id) {
  FormUserLoginFormAlter::hook($form, $form_state, $form_id);
}

/**
 * Implements hook_theme().
 */
function gdrp_compliance_theme() {
  return [
    'gdrp-popup' => [
      'variables' => [
        'data' => [],
      ],
    ],
  ];
}
