# Schema for the configuration files of the Tfa Duo module.
key.type.duo:
  type: sequence

tfa.validation.plugin.config.tfa_duo:
  type: mapping
  label: 'TFA Duo Settings'
  mapping:
    duo_key:
      type: string
      label: 'The Duo key containing connection information from Duo.com'
