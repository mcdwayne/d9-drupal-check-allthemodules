{#
/**
 * @file
 *
 * Default template for price lists.
 *
 * Available variables:
 * - attributes: HTML attributes for the wrapper.
 * - price_list: The rendered price list fields.
 *   Use 'price_list' to print them all, or print a subset such as
 *   'price_list.name'. Use the following code to exclude the
 *   printing of a given field:
 *   @code
 *   {{ price_list|without('name') }}
 *   @endcode
 * - price_list_entity: The price list entity.
 *
 * @ingroup themeable
 */
#}
<article{{ attributes }}>
  {{ price_list }}
</article>
