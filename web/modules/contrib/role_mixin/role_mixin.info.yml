name: Role mixin
type: module
description: 'The role mixin module allows to configure parent roles on roles. These parent roles then inherit the permissions of those sub roles.'
package: Other
core: 8.x
dependencies:
  - user
