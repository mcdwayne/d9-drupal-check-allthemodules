<?php

/**
 * @file
 * Contains Hooks.
 */

use Drupal\iots_channel\Hook\EntityView;

/**
 * Implements hook_ENTITY_TYPE_view().
 */
function iots_channel_iots_channel_view(array &$build, $entity, $display, $view_mode) {
  EntityView::hook($build, $entity, $view_mode);
}
