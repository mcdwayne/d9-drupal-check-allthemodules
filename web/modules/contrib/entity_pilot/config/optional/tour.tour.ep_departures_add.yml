langcode: en
status: true
dependencies:
  module:
    - entity_pilot
id: ep_departures_add
label: 'Add a departure'
module: entity_pilot
routes:
  -
    route_name: entity.ep_account.create_departure
  -
    route_name: entity_pilot.departure_add_page
tips:
  intro:
    id: intro
    plugin: text
    label: 'About departures'
    body: "Each Entity Pilot departure is a group of content that can be sent to Entity Pilot for importing in bulk on another site."
    weight: 1
  description:
    id: description
    plugin: text
    label: 'Description'
    body: 'Use the description field to give this depature a name. This is used in creating an arrival, so use a name that will make it easy to recognise when importing on another site.'
    weight: 2
    attributes:
      data-id: edit-info-0-value
  passengers:
    id: passengers
    plugin: text
    label: 'Passengers'
    body: "Use the widget to add the content (passengers) you wish to send to Entity Pilot. Note that <em>dependencies are automatically added</em>, so you don't have to add an image used in a piece of content, or the author, or any terms a piece of content is tagged with. Adding the content will ensure those are added as dependencies.<br><br>Use the <em>Add another item</em> button to add additional items."
    weight: 3
    attributes:
      data-id: passenger-list-add-more-wrapper
  entity_type:
    id: entity_type
    plugin: text
    label: 'Entity Type'
    body: "To select a piece of content (an entity), first select its type from the list."
    weight: 4
    attributes:
      data-id: edit-passenger-list-0-target-type
  entity:
    id: entity
    plugin: text
    label: 'Select content'
    body: "Start typing the content title or name to find items to send."
    weight: 5
    attributes:
      data-id: edit-passenger-list-0-target-id
