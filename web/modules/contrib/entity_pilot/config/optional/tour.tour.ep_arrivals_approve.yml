langcode: en
status: true
dependencies:
  module:
    - entity_pilot
id: ep_arrivals_approve
label: 'Approve an arrival'
uuid: 42b08390-89be-497f-9f09-094082a5c474
module: entity_pilot
routes:
  -
    route_name: entity.ep_arrival.approve_form
tips:
  intro:
    id: intro
    plugin: text
    label: 'Approving arrivals'
    body: 'Use this form to preview and approve the incoming content.'
    weight: 1
  preview:
    id: preview
    plugin: text
    label: 'Preview'
    body: 'Preview the incoming items'
    weight: 2
    location: left
    attributes:
      data-class: field--widget-ep-approved-passengers th:nth-child(5)
  existing:
    id: existing
    plugin: text
    label: 'Existing'
    body: 'If the incoming content matches an existing item on your site, you can view the existing item from this column.'
    weight: 3
    location: left
    attributes:
      data-class: field--widget-ep-approved-passengers th:nth-child(6)
  diff:
    id: diff
    plugin: text
    label: 'Diff'
    body: 'If the incoming content matches an existing item on your site, you can view the difference between the existing and incoming item here.'
    weight: 4
    location: left
    attributes:
      data-class: field--widget-ep-approved-passengers th:nth-child(7)
  select:
    id: select
    plugin: text
    label: 'Approve items'
    body: "Tick each of the items you wish to import. If you choose to import some items, references may not be resolved. For example if you import a piece of content, but not the terms it is tagged with, the imported piece of content will lose the tag. <br/><br/>If you choose not to import an item because you have an existing version, references will continue to resolve and will use the original version."
    weight: 5
    attributes:
      data-class: field--widget-ep-approved-passengers th:first-child()
  approve:
    id: approve
    plugin: text
    label: 'Approve'
    body: 'To approve an arrival but not import it, choose approve. You can queue it for import at a later date.'
    weight: 6
    attributes:
      data-id: edit-submit
  queue:
    id: queue
    plugin: text
    label: 'Queue'
    body: 'To approve an arrival and import it on the next cron run, choose queue.'
    weight: 7
    attributes:
      data-id: edit-queue
  land:
    id: land
    plugin: text
    label: 'Queue and land'
    body: 'To approve an arrival and import it immediately, choose queue and land.'
    weight: 8
    attributes:
      data-id: edit-land
