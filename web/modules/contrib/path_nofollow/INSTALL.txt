How to install:
- Enable the module in the drupal module setup page
- Enter paths at the setup page of the path_nofollow module
  (admin/settings/path_nofollow)
- Alter the page.tpl.php, include $body_attributes inside the body tag, like
  this:
  <body class="<?php print $body_classes; ?>"<?php print $body_attributes; ?>>