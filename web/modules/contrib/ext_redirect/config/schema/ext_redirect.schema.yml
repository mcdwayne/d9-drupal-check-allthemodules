ext_redirect.settings:
  type: config_object
  label: 'External Redirect Settings'
  mapping:
    primary_host:
      label: 'Primary host'
      type: uri
    allowed_host_aliases:
      label: 'Allowed host aliases'
      type: sequence
      sequence:
        label: 'Allowed host alias'
        type: uri