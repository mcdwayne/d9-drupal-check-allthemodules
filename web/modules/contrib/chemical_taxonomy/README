The Source data comes from the Classyfire project. To ceate the taxonomy terms, I followed the following procedure:
Download the data:
wget http://classyfire.wishartlab.com/system/downloads/1_0/chemont/ChemOnt_2_1.obo.zip
unzip ChemOnt_2_1.obo.zip

Open the file to edit:
vi ChemOnt_2_1.obo
:%s/^id: CHEMONTID:\(.*\)/'tid' => '\1',/g
:%s/^name: \(.*\)/'name' => "\1",/g
:%s/^def: "\(.*\)" \[.*\]/'description' => "\1",/g
:%s/is_a: CHEMONTID:\(.*\) \!.*/'parent' => '\1',/g
g!/^\'/d
:%s/^'tid'\(.*\)/array\(\r'tid'\1/g
:%s/^'parent'\(.*\)/'parent'\1\r\),/g

I then created the chemical_taxonomy.module file with no taxonomy terms, but with the string INSERT in the spot they should
be inserted. The following merges the data into the module file.
perl -pe 's/INSERT/`cat ChemOnt_2_1.obo`/ge' -i chemical_taxonomy.module
