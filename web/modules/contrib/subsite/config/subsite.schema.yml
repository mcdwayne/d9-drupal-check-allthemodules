# Schema for the configuration files of the schema module.

subsite.settings:
  type: config_object
  label: 'Subsite settings'
  mapping:
    allowed_types:
      type: sequence
      label: 'Content types that can be used as subsites'
      sequence:
        type: string
        label: 'Content type'
