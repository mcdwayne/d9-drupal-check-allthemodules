# Using ThirdPartySettingsInterface to store our config directly into Role
# config.
user.role.*.third_party.role_expose:
  type: mapping
  label: 'Role Expose setting'
  mapping:
    role_expose:
      type: integer
      label: 'How role is exposed'
