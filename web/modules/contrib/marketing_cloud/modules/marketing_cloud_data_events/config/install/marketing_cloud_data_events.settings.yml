definitions:
  insert_data_extension_rows_by_key:
    method: "post"
    endpoint: "/hub/v1/dataevents/[key]/rowset"
    schema: |
      {
        "$schema": "http://json-schema.org/draft-06/schema#",
        "title": "Insert data extension rows by key",
        "description": "Inserts a batch of data extensions rows by key.",
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "keys": {
              "type": "object"
            },
            "values": {
              "type": "object"
            }
          }
        },
        "additionalProperties": false
      }
  insert_data_extension_row_by_key:
    method: "put"
    endpoint: "/hub/v1/dataevents/[key]/rows/[primaryKeys]"
    schema: |
      {
        "$schema": "http://json-schema.org/draft-06/schema#",
        "title": "Insert data extension row by key",
        "description": "Inserts a data extension row by key.",
        "type": "object",
        "properties" : {
          "values": {
            "type": "object"
          }
        },
        "additionalProperties": false
      }
  increment_column_value_by_data_extension_key:
    method: "put"
    endpoint: "/hub/v1/dataevents/[key]/rows/[primaryKeys]/column/[column]/increment"
    schema: |
      {
        "$schema": "http://json-schema.org/draft-06/schema#",
        "title": "Increment column value by data extension key",
        "description": "Increments a column value by data extension external key.",
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
