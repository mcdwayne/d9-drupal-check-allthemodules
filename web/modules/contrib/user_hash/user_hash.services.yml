services:
  user_hash.services:
    class: Drupal\user_hash\Services
    arguments: ['@config.factory', '@user.data', '@entity.manager']
  user_hash.authentication.user_hash:
    class: Drupal\user_hash\Authentication\Provider\UserHash
    arguments: ['@config.factory', '@user.auth', '@flood', '@entity.manager']
    tags:
      - { name: authentication_provider, provider_id: 'user_hash', priority: 100 }
