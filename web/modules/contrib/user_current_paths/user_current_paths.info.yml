name: 'User current paths'
description: 'Provides UID neutral user action paths (user/current/*) and redirects to the current user path by replacing current with the UID. Also adds an "Edit my account" link to the user menu for logged in users.'
type: module
core: 8.x
dependencies:
  - drupal:user