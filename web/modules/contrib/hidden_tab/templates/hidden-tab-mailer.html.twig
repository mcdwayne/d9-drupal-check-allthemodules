{#
/**
 * @file
 * Default theme implementation to present a hidden tab mailer entity.
 *
 * This template is used when viewing a registered hidden tab mailer's page,
 * e.g., /admin/content/hidden-tab-mailer)/123. 123 being the hidden tab mailer's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_hidden_tab_mailer()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
