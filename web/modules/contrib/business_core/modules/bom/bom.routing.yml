bom.bom_component:
  path: '/admin/bom/{bom}/component'
  requirements:
    _entity_access: 'bom.view'

bom.bom_component.add_form:
  path: '/admin/bom/{bom}/component/add'
  default:
    _entity_form: 'bom.default'
    _title: 'Add component'
  requirements:
    _entity_access: 'bom.update'
