stripe_checkout.stripe_charge_controller_charge:
  path: '/stripe_checkout/charge'
  methods: [POST]
  defaults:
    _controller: '\Drupal\stripe_checkout\Controller\StripeChargeController::charge'
    _title: 'charge'
  requirements:
    _permission: 'access content'
