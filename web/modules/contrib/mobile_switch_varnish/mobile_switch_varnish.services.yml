services:
  theme.negotiator.mobile_switch_varnish:
    class: Drupal\mobile_switch_varnish\Theme\MobileSwitchThemeNegotiator
    arguments: ['@config.factory', '@router.admin_context']
    tags:
      - { name: theme_negotiator, priority: 0 }