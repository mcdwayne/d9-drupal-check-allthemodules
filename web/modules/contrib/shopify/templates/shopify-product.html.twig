{#
/**
 * @file
 * Default theme implementation to display a product.
 *
 * Available variables:
 * - content: All product items. Use {{ content }} to print them all.
 * - attributes: HTML attributes for the containing element.
 * - view_mode: View mode; for example, "teaser" or "store_listing".
 * - shopify_product_variant: The shopify product object.
 *
 * @ingroup themeable
 */
#}
{%
set classes = [
'shopify-product',
view_mode ? 'shopify-product--view-' ~ view_mode|clean_class,
]
%}
<article{{ attributes.addClass(classes) }}>
  {{ content }}
</article>
