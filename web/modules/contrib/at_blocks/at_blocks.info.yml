name: 'AT Blocks'
type: module
description: 'Provide page elements as blocks for testing. Deprecate when Drupal core provides these blocks. See issue: #507488 (Convert page elements (title, tabs, actions) into blocks)'
package: Adaptivetheme
core: 8.x
version: 8.x-1.x
dependencies:
  - block
