# DCS - Introduction
entity.drupal_content_sync.introduction:
  path: '/admin/config/services/drupal_content_sync/introduction'
  defaults:
    _controller: 'Drupal\drupal_content_sync\Controller\DrupalContentSyncIntroduction::content'
    _title: 'Drupal Content Sync - Introduction'
  requirements:
    _permission: 'administer drupal content sync:'

# DCS - Flows
entity.dcs_flow.collection:
  path: '/admin/config/services/drupal_content_sync/flow'
  defaults:
    _entity_list: 'dcs_flow'
    _title: 'Flows - Drupal Content Sync'
  requirements:
    _permission: 'administer drupal content sync:'

entity.dcs_flow.add_form:
  path: '/admin/config/services/drupal_content_sync/flow/add'
  defaults:
    _entity_form: 'dcs_flow.add'
    _title: 'Add Flow to Drupal Content Sync'
  requirements:
    _permission: 'administer drupal content sync:'

entity.dcs_flow.edit_form:
  path: '/admin/config/services/drupal_content_sync/flow/{dcs_flow}/edit'
  defaults:
    _entity_form: 'dcs_flow.edit'
    _title: 'Edit Flow from Drupal Content Sync'
  requirements:
    _permission: 'administer drupal content sync:'

entity.dcs_flow.delete_form:
  path: '/admin/config/services/drupal_content_sync/flow/{dcs_flow}/delete'
  defaults:
    _entity_form: 'dcs_flow.delete'
    _title: 'Delete Flow from Drupal Content Sync'
  requirements:
    _permission: 'administer drupal content sync:'

entity.dcs_flow.export:
  path: '/admin/config/services/drupal_content_sync/flow/{dcs_flow}/export'
  defaults:
    _controller: '\Drupal\drupal_content_sync\Controller\FlowExport::export'
    _title: 'Export Flow'
  requirements:
    _permission: 'administer drupal content sync:'

# DCS - Pools
entity.dcs_pool.collection:
  path: '/admin/config/services/drupal_content_sync/pool'
  defaults:
    _entity_list: 'dcs_pool'
    _title: 'Pools - Drupal Content Sync'
  requirements:
    _permission: 'administer drupal content sync:'

entity.dcs_pool.add_form:
  path: '/admin/config/services/drupal_content_sync/pool/add'
  defaults:
    _entity_form: 'dcs_pool.add'
    _title: 'Add Pool to Drupal Content Sync'
  requirements:
    _permission: 'administer drupal content sync:'

entity.dcs_pool.edit_form:
  path: '/admin/config/services/drupal_content_sync/pool/{dcs_pool}/edit'
  defaults:
    _entity_form: 'dcs_pool.edit'
    _title: 'Edit Pool from Drupal Content Sync'
  requirements:
    _permission: 'administer drupal content sync:'

entity.dcs_pool.delete_form:
  path: '/admin/config/services/drupal_content_sync/pool/{dcs_pool}/delete'
  defaults:
    _entity_form: 'dcs_pool.delete'
    _title: 'Delete Pool from Drupal Content Sync'
  requirements:
    _permission: 'administer drupal content sync:'

entity.dcs_pool.export:
  path: '/admin/config/services/drupal_content_sync/pool/{dcs_pool}/export'
  defaults:
    _controller: '\Drupal\drupal_content_sync\Controller\PoolExport::export'
    _title: 'Export Pool'
  requirements:
    _permission: 'administer drupal content sync:'

drupal_content_sync.dcs_pool.pool_required:
  path: '/admin/config/services/drupal_content_sync/flow/pool-required'
  defaults:
    _form: '\Drupal\drupal_content_sync\Form\PoolRequired'
  requirements:
    _permission: 'publish drupal content sync changes'

# General settings
drupal_content_sync.settings_form:
  path: '/admin/config/services/drupal_content_sync/settings'
  defaults:
    _form: '\Drupal\drupal_content_sync\Form\SettingsForm'
    _title: 'Settings - Drupal Content Sync'
  requirements:
    _permission: 'administer drupal content sync:'

# Debug form
drupal_content_sync.dcs_debug_form:
  path: '/admin/config/services/drupal_content_sync/debug'
  defaults:
    _form: '\Drupal\drupal_content_sync\Form\DebugForm'
    _title: 'Debug - Drupal Content Sync'
  requirements:
    _permission: 'administer drupal content sync:'

# DCS - Push changes
drupal_content_sync.publish_changes:
  path: '/drupal-content-sync/publish-changes/{flow_id}/{entity}/{entity_type}'
  defaults:
    _controller: '\Drupal\drupal_content_sync\Controller\DrupalContentSyncPushChanges::pushChanges'
  requirements:
    _permission: 'publish drupal content sync changes'
  options:
    parameters:
      flow_id:
        type: string
      entity:
        type: integer
      entity_type:
        type: string

drupal_content_sync.publish_changes_entities:
  path: '/drupal-content-sync/publish-changes/entities'
  defaults:
    _controller: '\Drupal\drupal_content_sync\Controller\DrupalContentSyncPushChanges::pushChangesEntitiesList'
  requirements:
    _permission: 'access content'

node.drupal_content_sync_export_publish_changes_confirm:
  path: '/admin/content/push-changes/confirm'
  defaults:
    _form: '\Drupal\drupal_content_sync\Form\DrupalContentSyncPushChangesConfirm'
  requirements:
    _permission: 'publish drupal content sync changes'

entity.drupal_content_sync.content:
  path: '/admin/content/import'
  defaults:
    _controller: '\Drupal\drupal_content_sync\Controller\ManualImportController::content'
    _title: 'Import Content'
  requirements:
    _permission: 'access drupal content sync content overview'
