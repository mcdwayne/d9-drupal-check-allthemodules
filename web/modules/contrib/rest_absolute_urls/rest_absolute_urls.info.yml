name: REST Absolute URLs
description: Replaces relative URLs with absolute URLs for all content exposed via any REST service.

type: module
# core: 8.x

dependencies:
  - drupal:serialization

# Information added by Drupal.org packaging script on 2017-08-15
version: '8.x-1.0-beta1'
core: '8.x'
project: 'rest_absolute_urls'
datestamp: 1502800446
