# Schema for the configuration files of the simplefader module.

simplefader.settings:
  type: config_object
  label: 'SimpleFader settings'
  mapping:
    simplefader_selected_element:
      type: string
      label: 'Selected Element'