<?php

/**
 * @file
 * Provides views handlers.
 */

/**
 * Implements hook_views_data_alter().
 *
 * @param array $data
 */
function views_math_views_data_alter(array &$data) {

  $data['views']['views_math_area_total'] = [
    'title' => t('Total for the field'),
    'help' => t('Shows the total for numeric fields.'),
    'area' => [
      'id' => 'views_math_area_total',
    ],
  ];

}