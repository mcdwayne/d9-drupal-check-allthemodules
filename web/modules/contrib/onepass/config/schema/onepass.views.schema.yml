# Schema for the views plugins of the Onepass module.

views.access.onepassonly:
  type: mapping
  label: 'OnePass access only'

views.pager.onepasspager:
  type: views_pager
  label: 'OnePass atoms pager'

views.style.onepassatoms:
  type: views_style
  label: 'OnePass atoms feed'
