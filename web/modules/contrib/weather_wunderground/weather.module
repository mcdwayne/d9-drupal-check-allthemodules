<?php
/**
 * Implements hook_theme().
 */
function weather_theme($existing, $type, $theme, $path) {
  return [
    'weather_block' => [
      'variables' => ['weather' => NULL],
      'template' => 'weather',
    ],
  ];
}
