services:
  address.checkout:
    class: Drupal\address_checkout\Helper\AddressCheckoutHelper
    arguments: ['@entity_type.manager', '@current_user', '@request_stack', '@path.current', '@string_translation']
