services:
  logger.watchdog_slack:
    class: Drupal\watchdog_slack\Logger\SlackLog
    arguments: ['@logger.log_message_parser']
    tags:
      - { name: logger }
      - { name: backend_overridable }
