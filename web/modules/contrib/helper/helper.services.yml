services:
  logger.channel.helper:
    parent: logger.channel_base
    arguments: ['helper']
  helper.config:
    class: Drupal\helper\Config
    arguments: ['@config.factory', '@config.manager', '@logger.channel.helper']
  helper.file:
    class: Drupal\helper\File
    arguments: ['@entity_type.manager']
