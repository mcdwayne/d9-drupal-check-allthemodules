field.widget.settings.uppy_widget:
  type: mapping
  label: 'Uppy widget settings'
  mapping:
    auto_proceed:
      type: boolean
      label: 'Upload immediately'
    file_sources:
      type: string
      label: 'File Sources'
    uploader:
      type: string
      label: 'Uploader'
    chunk_size:
      type: integer
      label: 'Chunk size in bytes'
