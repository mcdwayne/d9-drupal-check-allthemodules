services:
  tarpit.config.override:
    class: \Drupal\tarpit\Config\ConfigOverride
    tags:
      - {name: config.factory.override, priority: 0}
  logger.channel.tarpit:
    parent: logger.channel_base
    arguments: ['tarpit']