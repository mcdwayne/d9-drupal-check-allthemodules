tmgmt.translator.settings.xconnect:
  type: mapping
  mapping:
    auto_accept:
      type: boolean
      label: Automatically accept
    order:
      client_id:
        type: string
        label: Cliend ID
      is_confidential:
        type: boolean
        label: Is confidential
      needs_confirmation:
        type: boolean
        label: Needs confirmation
      needs_quotation:
        type: boolean
        label: Needs quotation
    connection:
      protocol:
        type: string
        label: Protocol
      port:
        type: integer
        label: Port number
      timeout:
        type: integer
        label: Timeout
      hostname:
        type: string
        label: Hostname
      username:
        type: string
        label: Username
      password:
        type: string
        label: Password
      directory_send:
        type: string
        label: Request folder
      directory_send_processed:
        type: string
        label: Processed request folder
      directory_receive:
        type: string
        label: Receive folder
      directory_receive_processed:
        type: string
        label: Processed receive folder
    cron:
      status:
        type: boolean
        label: Cron status
      limit:
        type: integer
        label: Cron limit
    debug:
      status:
        type: boolean
        label: Debug status
