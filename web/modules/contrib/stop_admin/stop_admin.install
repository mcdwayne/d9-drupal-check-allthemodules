<?php

/**
 * @file
 * Install functions for the Stop Administrator Login module.
 */

/**
 * Set the configuration for existing sites.
 */
function stop_admin_update_8001() {
  \Drupal::configFactory()
    ->getEditable('stop_admin.settings')
    ->set('disabled', FALSE)
    ->save(TRUE);
}
