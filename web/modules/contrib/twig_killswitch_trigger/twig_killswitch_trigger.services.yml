services:
  twig_killswitch_trigger.twig.extension:
    class: Drupal\twig_killswitch_trigger\TwigExtension\KillswitchTriggerTwigExtension
    arguments: ['@renderer', '@page_cache_kill_switch']
    tags:
      - { name: twig.extension }
