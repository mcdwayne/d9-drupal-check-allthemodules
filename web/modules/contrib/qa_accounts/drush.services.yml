services:
  qa_accounts.commands:
    class: \Drupal\qa_accounts\Commands\QaAccountsCommands
    arguments:
      - '@entity_type.manager'
      - '@logger.factory'
    tags:
      - { name: drush.command }
