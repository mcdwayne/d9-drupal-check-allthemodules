<?php

/**
 * @file
 * A collection of module hooks for the flot_views_time_examples module.
 */

/**
 * Implements hook_foo_BAR_ID_bar() for xyz_bar().
 */
function flot_views_time_examples_flot_examples_toc_alter(&$item_list) {
  $url = '/flot-examples/flot-views-time/log-time-chart';
  $options = [
    ':one' => $url,
  ];
  $item_list[] = t('<a href=":one">Time-series chart views formatter</a>', $options);
}
