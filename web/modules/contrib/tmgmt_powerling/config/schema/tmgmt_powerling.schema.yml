tmgmt.translator.settings.powerling:
  type: tmgmt.translator_base
  mapping:
    token:
      type: string
      label: Powerling Web Token
    use_sandbox:
      type: boolean
      label: Use the sandbox

