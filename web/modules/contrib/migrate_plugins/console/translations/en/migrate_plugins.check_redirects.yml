description: 'Checks a CSV list of migrated redirects and report the URLS that are missing redirect.'
options:
  csv-file: 'Filename of CSV migration redirects list.'
  redirect-column: 'The CSV column name that contains the source URIs of the redirects.'
arguments: {}
