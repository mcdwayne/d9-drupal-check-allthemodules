download.download:
  path: '/download/{bundle}/{entity_type}/{fieldname}/{entity_id}/{delta}'
  defaults:
    _controller: '\Drupal\download\Controller\DefaultController::download_download'
  requirements:
    _permission: 'download zip via download link'
