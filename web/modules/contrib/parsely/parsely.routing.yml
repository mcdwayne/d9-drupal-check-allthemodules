parsely.admin_settings:
  path: /admin/config/search/parsely
  defaults:
    _title: Parse.ly
    _form: \Drupal\parsely\Form\ParselyAdminSettings
  requirements:
    _permission: 'administer site configuration'