user_sanitize.user_sanitize_config:
  path: '/admin/config/user_sanitize'
  defaults:
    _form: '\Drupal\user_sanitize\Form\UserSanitizeConfigForm'
    _title: 'User Sanitize'
  requirements:
    _permission: 'administer site configuration'
