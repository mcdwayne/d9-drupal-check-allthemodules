services:
  media_imagick.plugindecorator.media.source:
    # This is the (arbitrarily named) magick service declaration that tells
    # plugindecorator.module that it shall prepare media.source plugins to be
    # decorated.
    abstract: true
    tags:
      - { name: plugindecorator, manager: media.source }
