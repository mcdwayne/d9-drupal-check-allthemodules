name: Term Split
description: Allows taxonomy terms to be split into two and choose where each reference is migrated.
core: 8.x
type: module

dependencies:
  - drupal:taxonomy
  - drupal:node
  - drupal:text
  - drupal:field
  - drupal:filter
  - drupal:user
  - drupal:system
  - hook_event_dispatcher:hook_event_dispatcher
  - term_reference_change:term_reference_change
