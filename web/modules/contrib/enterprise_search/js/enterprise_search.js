/**
 * @file
 * Expands the behaviour of the default autocompletion.
 */
(function ($) {
  Drupal.autocomplete.options.minLength = 3;
  console.log(drupalSettings.enterprise_search.min_length);
})(jQuery);