isoregistry.config:
    path: '/admin/structure/isoregistry'
    defaults:
      _form: '\Drupal\isoregistry\Form\ConfigForm'
      _title: 'ISORegistry Configuration'
    requirements:
      _permission: 'administer site configuration'