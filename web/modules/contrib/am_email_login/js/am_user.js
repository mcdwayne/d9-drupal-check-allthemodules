(function ($, Drupal, document) {
  'use strict';

  Drupal.behaviors.am_registration = {
    attach: function(context, settings) {
      // $('main', context).once('awesome').append('<p>Hello world</p>');
      alert("hi");
    }
  };

}(jQuery));