passwd_only.all:
  type: config_object
  label: 'Configuration of the module password only login'
  mapping:
    user:
      type: integer
      label: 'The User ID (uid) of the account which is used to login.'
    description:
      type: text
      label: 'This description text is rendered on the login form.'
