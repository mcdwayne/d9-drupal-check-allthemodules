# Schema for the configuration files of the Neutral Paths module.

neutral_paths.settings:
  type: config_object
  label: 'Neutral Paths settings'
  mapping:
    neutral_paths_fix_new:
      type: sequence
      label: 'Types of paths to set as language neutral'
      sequence:
        type: string
