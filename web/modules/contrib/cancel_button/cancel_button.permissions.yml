administer cancel button configuration:
  title: 'Administer Cancel button configuration'
  description: "Configure the per-content type redirect location to use as a fallback for the cancel button when no form state redirect, destination parameter, or HTTP referer is available, and the node doesn't exist yet."
