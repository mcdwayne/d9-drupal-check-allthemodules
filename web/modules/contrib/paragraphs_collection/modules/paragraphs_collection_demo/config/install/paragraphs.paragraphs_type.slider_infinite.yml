langcode: en
status: true
dependencies:
  module:
    - slick
    - paragraphs_collection
  enforced:
    module:
      - paragraphs_collection_demo
id: slider_infinite
label: 'Slider Infinite'
description: 'Use <em>Slider Infinite</em> to display paragraphs as a slick infinite slider.'
behavior_plugins:
  slider:
    enabled: true
    field_name: field_slides
    slick_slider:
      - slider_infinite
