langcode: en
status: true
dependencies:
  module:
    - paragraphs_collection
  enforced:
    module:
      - paragraphs_collection
id: image_text
label: 'Image + Text'
description: 'Use <em>Image + Text</em> to add an image with a text.'
