langcode: en
status: true
dependencies:
  module:
    - paragraphs_collection
  enforced:
    module:
      - paragraphs_collection
id: quote
label: Quote
description: 'Use <em>Quote</em> to add a quote with its author.'
