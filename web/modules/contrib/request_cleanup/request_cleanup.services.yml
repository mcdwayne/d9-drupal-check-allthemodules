services:
  request_cleanup.middleware:
    class: Drupal\request_cleanup\StackMiddleware\Cleanup
    arguments: ['@settings']
    tags:
      # priority must be higher than 200, to work before PageCache
      - { name: http_middleware, priority: 300, responder: true }
