name: 'Widget On Demand'
type: module
description: 'Provides a view element instead of a form element and on click (on demand) loads the corresponding form element. It is also a framework for other modules to create widgets on demand.'
package: Fields
core: 8.x
dependencies:
  - field