name: jsTree menu
description: Allows to render menus with jsTree library via custom blocks
type: module
configure: jstree_menu.config

core: 8.x

dependencies:
  - libraries

library_dependencies:
  - jstree
