
Switch back to using DIV's instead of JQuery popups.
The popups are being loaded dynamically, add the html to #imager-wrapper
Initially it will be hidden, display it through JS.  Make it's position separate.
Add a wrapper to make it resizable/draggable - first get draggable working.

Have to redo the way buttons are done.
Look at the code in:w

