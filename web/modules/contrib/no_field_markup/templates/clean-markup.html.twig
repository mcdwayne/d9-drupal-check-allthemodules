{#
/**
 * @file
 * Theme implementation for a field.
 *
 * To clean markup of field using this template.
 *
 * Available variables:
 * - items: List of all the field items. Each item contains:
 *   - content: The field item's content.
 *
 * @see template_preprocess_clean_markup()
 *
 * @ingroup themeable
 */
#}
{% for item in items %}
  {{ item.content }}
{% endfor %}
