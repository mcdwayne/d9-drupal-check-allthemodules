services:
  authentication.skyword_auth:
    class: Drupal\skyword\Authentication\Provider\SkywordAuth
    arguments: ['@config.factory', '@entity.manager']
    tags:
      - { name: authentication_provider, provider_id: skyword, priority: 100 }