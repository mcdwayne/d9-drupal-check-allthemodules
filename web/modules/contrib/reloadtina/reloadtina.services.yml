services:
  reloadtina.route_subscriber:
    class: Drupal\reloadtina\Routing\RouteSubscriber
    tags:
      - { name: event_subscriber }
  reloadtina.image_style:
    class: Drupal\reloadtina\ParamConverter\ReloadTinaParamConverter
    tags:
      - { name: paramconverter }
