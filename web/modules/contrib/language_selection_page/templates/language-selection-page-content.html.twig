{#

/**
 * @file
 * The body template file of the language_selection_page module.
 *
 * Available variables:
 *  - configure_url: the configuration page for the module.
 *  - destination: url of the page without a language set.
 *  - language-links: links to the page in all available language.
 */

#}

<div class="language_selection_page_body">
    <div class="language_selection_page_body_inner">
        <p>No language has been detected and you are coming from <b>{{ destination }}</b></p>

        <p>You should go to the page in:</p>
        {{ language_links }}
        <p>This page is the default page of the module Language Selection Page, you can <a href="{{ configure_url }}">configure the module</a> to alter its behavior.</p>
    </div>
</div>
