langcode: en
status: true
dependencies:
  config:
    - cloud.cloud_config_type.aws_ec2
    - field.storage.cloud_config.field_use_instance_credentials
id: cloud_config.aws_ec2.field_use_instance_credentials
field_name: field_use_instance_credentials
entity_type: cloud_config
bundle: aws_ec2
label: 'Use instance credentials'
description: 'This option is only available if cloud module is on an ec2 instance.  If checked, there is no need to supply access key or secret key.  The credentials are delivered through the Amazon EC2 metadata service.'
required: false
translatable: false
default_value:
  -
    value: 0
default_value_callback: ''
settings:
  on_label: 'On'
  off_label: 'Off'
field_type: boolean
