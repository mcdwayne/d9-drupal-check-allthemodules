{#
/**
 * @file cloud_config.html.twig
 * Default theme implementation to present Cloud config data.
 *
 * This template is used when viewing Cloud config pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_cloud_config()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('cloud_config') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
