<?php

/**
 * Implements hook_theme().
 */
function syslog_report_theme($existing, $type, $theme, $path) {
  return [
    'syslog_report' => [
      'variables' => ['log_report' => NULL],
    ],
  ];
}
