function turn_on_changed_flag(id) {
  jQuery('#edit-tasks-table-' + id + '-change').prop('checked', true);
}

function turn_on_changed_flag_vars(id) {
  jQuery('#edit-vars-table-' + id + '-change').prop('checked', true);
}