services:
  chatbot_slack.slack_service:
    class: Drupal\chatbot_slack\Service\SlackService
    arguments: ['@http_client', '@logger.channel.chatbot', '@request_stack']
  chatbot_slack.workflow:
    class: Drupal\chatbot_slack\Workflow\SlackBotWorkflow
    arguments: ['@config.factory', '@chatbot.conversation_factory', '@string_translation', '@chatbot_slack.slack_service', '@logger.channel.chatbot']
