route_callbacks:
  - '\Drupal\chatbot\Routing\ChatbotRoutes::routes'

chatbots.items:
  path: '/admin/structure/chatbots'
  defaults:
    _controller: '\Drupal\chatbot\Controller\ChatbotController::chatbotsMenuItemListings'
    _title: 'Chatbots'
  requirements:
    _permission: 'access administration pages'
