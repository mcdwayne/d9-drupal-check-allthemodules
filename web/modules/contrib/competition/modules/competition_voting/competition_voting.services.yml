services:
  competition.voting:
    class: Drupal\competition_voting\CompetitionVoting
    arguments: ['@entity_type.manager', '@database', '@current_user', '@supercookie.manager', '@supercookie.response', '@request_stack']
