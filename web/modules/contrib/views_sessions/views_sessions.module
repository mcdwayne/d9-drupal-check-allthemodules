<?php

/**
 * @file
 * Provide views data for views_sessions module.
 */

/**
 * Implements hook_views_data().
 */
function nepo_custom_views_data() {
  $data = [];

  $data['sessions']['table']['group'] = t('Sessions');
  $data['sessions']['table']['wizard_id'] = 'sessions';

  $data['sessions']['table']['base'] = [
    'field' => 'sid',
    'title' => t('Active sessions'),
    'help' => t('Contains a list of active sessions.'),
  ];

  $data['sessions']['sid'] = [
    'title' => t('SID'),
    'help' => t('Unique session ID.'),
    'field' => [
      'id' => 'standard',
    ],
    'filter' => [
      'id' => 'string',
    ],
    'argument' => [
      'id' => 'string',
    ],
    'sort' => [
      'id' => 'standard',
    ],
  ];

  $data['sessions']['uid'] = [
    'title' => t('UID'),
    'help' => t('The user ID for the active session.'),
    'field' => [
      'id' => 'standard',
    ],
    'filter' => [
      'id' => 'numeric',
    ],
    'argument' => [
      'id' => 'numeric',
    ],
    'relationship' => [
      'title' => t('User'),
      'help' => t('The user on which the log entry as written.'),
      'base' => 'users_field_data',
      'base field' => 'uid',
      'id' => 'standard',
    ],
  ];

  $data['sessions']['hostname'] = [
    'title' => t('Hostname'),
    'help' => t('The IP address that last used this session ID.'),
    'field' => [
      'id' => 'standard',
    ],
    'argument' => [
      'id' => 'string',
    ],
    'filter' => [
      'id' => 'standard',
    ],
    'sort' => [
      'id' => 'standard',
    ],
  ];

  $data['sessions']['timestamp'] = [
    'title' => t('Timestamp'),
    'help' => t('The Unix timestamp when this session last requested a page.'),
    'field' => [
      'id' => 'date',
    ],
    'argument' => [
      'id' => 'date',
    ],
    'filter' => [
      'id' => 'date',
    ],
    'sort' => [
      'id' => 'date',
    ],
  ];

  $data['sessions']['session'] = [
    'title' => t('Session variables'),
    'help' => t('The variables of the session in a serialized format.'),
    'field' => [
      'id' => 'serialized',
      'click sortable' => FALSE,
    ],
    'argument' => [
      'id' => 'string',
    ],
    'filter' => [
      'id' => 'string',
    ],
    'sort' => [
      'id' => 'standard',
    ],
  ];

  return $data;
}
