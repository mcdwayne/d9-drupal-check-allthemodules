name: Form Block IP Address (FBIP)
type: module
description: Keeps a track of from submissions of all monitored forms and blocks IP address when thresholds are reached
dependencies:
  - ban
configure: fbip.fbip_settings_form
core: 8.x
package: Other
