name: _Config example
type: module
description: Provides examples for using the _config.module to add help text, alter forms, and add descriptions to roles.
package: AP
version: VERSION
core: 8.x
dependencies:
  - _config
