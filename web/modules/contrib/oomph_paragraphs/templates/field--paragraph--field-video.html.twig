{#
/**
 * @file
 * Theme override for a video paragraph source output.
 *
 * Uses Drupal's built in responsive video wrapper and CSS
 * We don't need to modify any further than this
 */
#}
{% for item in items %}
  {{ item.content }}
{% endfor %}
