langcode: en
status: true
dependencies:
  config:
    - group.type.kb_group
  module:
    - user
  enforced:
    module:
      - kb
id: kb_group-group_membership
label: 'KB Group: Group membership'
description: 'Adds users to groups as members.'
group_type: kb_group
content_plugin: group_membership
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true
