langcode: en
status: true
dependencies:
  config:
    - group.type.kb_group
    - node.type.kb_content
  module:
    - gnode
    - node
  enforced:
    module:
      - kb
id: kb_group-group_node-kb_content
label: 'KB Group: Group node (KB Content)'
description: 'Adds <em class="placeholder">KB Content</em> content to groups both publicly and privately.'
group_type: kb_group
content_plugin: 'group_node:kb_content'
plugin_config:
  group_cardinality: 1
  entity_cardinality: 1
  use_creation_wizard: true