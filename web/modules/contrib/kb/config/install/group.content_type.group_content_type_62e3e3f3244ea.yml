langcode: en
status: true
dependencies:
  config:
    - group.type.members_only_kb_group
    - node.type.kb_category
  module:
    - gnode
    - node
  enforced:
    module:
      - kb
id: group_content_type_62e3e3f3244ea
label: 'Members-only KB Group: Group node (KB Category)'
description: 'Adds <em class="placeholder">KB Category</em> content to groups both publicly and privately.'
group_type: members_only_kb_group
content_plugin: 'group_node:kb_category'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: true