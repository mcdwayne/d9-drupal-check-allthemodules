langcode: en
status: true
dependencies:
  config:
    - group.type.kb_group
    - node.type.kb_category
  module:
    - gnode
    - node
  enforced:
    module:
      - kb
id: kb_group-group_node-kb_category
label: 'KB Group: Group node (KB Category)'
description: 'Adds <em class="placeholder">KB Category</em> content to groups both publicly and privately.'
group_type: kb_group
content_plugin: 'group_node:kb_category'
plugin_config:
  group_cardinality: 1
  entity_cardinality: 1
  use_creation_wizard: true