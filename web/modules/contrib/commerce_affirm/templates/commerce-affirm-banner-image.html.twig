{#
/**
 * @file
 *
 * Theme implementation to present an Affirm banner image.
 *
 * Available variables:
 * - width: The img width attribute.
 * - banner_size: The banner size, like 120x60. Needed to generate the img src
 *   attribute. 
 */
#}
<img src="https://cdn-assets.affirm.com/images/banners/{{ banner_size }}.png" {% if width is not empty %} width="{{ width }}" {% endif %}/>
