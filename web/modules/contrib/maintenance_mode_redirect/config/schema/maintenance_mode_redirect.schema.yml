# Schema for the configuration files of the maintenance_mode_redirect module.

maintenance_mode_redirect.settings:
  type: mapping
  label: 'Maintenance Mode Redirect Settings'
  mapping:
    maintenance_mode_redirect_active:
      type: boolean
      label: 'Use redirect on maintenance mode'
    maintenance_mode_redirect_url:
      type: string
      label: 'Path to redirect users'
