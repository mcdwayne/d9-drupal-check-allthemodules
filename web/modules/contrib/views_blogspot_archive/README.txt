This module provides a blogger(blogspot) like archive style option for Views.

You can set the field based on which archive need to be created in views style settings.

Note: You can add custom styles to align the item list for default
core bartik theme.

-- INSTALLATION --

  Install module like normal.

-- CONFIGURATION --
  Follow example view attached for setting references(Block display for archive
  and Page display for views archive result).
