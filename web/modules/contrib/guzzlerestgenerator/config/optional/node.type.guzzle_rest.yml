# node.type.guzzle_rest.yml
langcode: en
status: true
dependencies:
  enforced:
    module:
      - guzzlerestgenerator
name: 'Guzzle Drupal Endpoint'
type: guzzle_rest
description: 'Content type that performs external REST calls and returns JSON output if available.'
help: ''
new_revision: false
preview_mode: 0
display_submitted: true