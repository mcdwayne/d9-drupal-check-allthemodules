---
base_url: 'http://127.0.0.1:8200'
plugin_auth: ''
# Default TTL increment of 2 days - giving us some leeway for a single failed
# cron task if running daily.
lease_ttl_increment: "172800"
lease_renew_cron: true
