entity.event_type.conflicts:
  path: '/admin/structure/rng/event_types/manage/{event_type}/conflicts'
  defaults:
    _entity_form: 'event_type.conflicts'
    _title: 'Conflicts'
  requirements:
    _permission: 'administer event types'
