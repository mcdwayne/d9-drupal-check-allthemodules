services:
  ssf_comment.route.subscriber:
    class: Drupal\ssf_comment\Routing\SsfRouteSubscriber
    tags:
      - { name: event_subscriber }

  ssf_comment.rating_event_subscriber:
    class: Drupal\ssf_comment\EventSubscriber\SsfCommentEventSubscriber
    arguments: ['@config.factory', '@plugin.manager.mail', '@renderer', '@current_user', '@language_manager']
    tags:
      - { name: event_subscriber }
