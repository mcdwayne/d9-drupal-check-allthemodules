ssf_comment set spam:
  title: Set comment as spam.
  description: Adds comment to the filter as spam and unpublishes the comment (when not already unpublished).
  restrict access: true
ssf_comment set ham:
  title: Set comment as ham.
  description: Adds comment to the filter as ham and publishes the comment (when not already published).
  restrict access: true
