<?php

use Drupal\Core\Extension\InfoParser;

/**
 * Implements hook_modules_installed().
 */
//function npm_modules_installed($modules) {
//  $moduleHandler = \Drupal::moduleHandler();
//  /** @var \Drupal\npm\Plugin\NpmExecutableInterface $npmExecutable */
//  $npmExecutable = \Drupal::service('plugin.manager.npm_executable')->getExecutable();
//  foreach ($modules as $module) {
//    /** @var \Drupal\Core\Extension\Extension $extension */
//    $extension = $moduleHandler->getModule($module);
//    $infoParser = new InfoParser();
//    $info = $infoParser->parse($extension->getPathname());
//    if (isset($info['npm_dependencies'])) {
//      $npmExecutable->addPackages($info['npm_dependencies']);
//    }
//  }
//}
