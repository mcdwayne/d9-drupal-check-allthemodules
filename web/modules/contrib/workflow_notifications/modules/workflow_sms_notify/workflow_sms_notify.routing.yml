entity.workflow_sms_notify.add:
  path: '/admin/config/workflow/workflow/{workflow_type}/sms-notifications/add'
  defaults:
    _entity_form: 'workflow_sms_notify.add'
    _title: 'Add Workflow SMS Notification'
  options:
    parameters:
      workflow_type:
        type: entity:workflow_type
    _admin_route: TRUE
  requirements:
    _entity_create_access: 'workflow_notify'
    
entity.workflow_sms_notify.edit_form:
  path: '/admin/config/workflow/workflow/{workflow_type}/sms-notifications/{workflow_sms_notify}/edit'
  defaults:
    _entity_form: 'workflow_sms_notify.edit'
    _title: 'Edit Workflow SMS Notification'
  options:
    parameters:
      workflow_type:
        type: entity:workflow_type
    _admin_route: TRUE
  requirements:
    _permission: 'administer workflow'

entity.workflow_sms_notify.delete_form:
  path: '/admin/config/workflow/workflow/{workflow_type}/sms-notifications/{workflow_sms_notify}/delete'
  defaults:
    _entity_form: 'workflow_sms_notify.delete'
    _title: 'Delete workflow SMS Notification'
  options:
    _admin_route: TRUE
    parameters:
      workflow_type:
        type: entity:workflow_type
  requirements:
    _permission: 'administer workflow'

entity.workflow_sms_notify.collection:
  path: '/admin/config/workflow/workflow/{workflow_type}/sms-notifications'
  defaults:
    _entity_list: 'workflow_sms_notify'
    _title: 'Manage Workflow SMS Notifications'
  options:
    _admin_route: TRUE
    parameters:
      workflow_type:
        type: entity:workflow_type
  requirements:
    _permission: 'administer workflow'
