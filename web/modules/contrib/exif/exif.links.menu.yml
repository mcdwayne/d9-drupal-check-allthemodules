exif.admin:
  description: "configure the entity types that will be check for metadata, the solution to extract the metadata"
  parent: system.admin_config_media
  route_name: exif.config
  title: "Image Metadata"
  weight: 100
