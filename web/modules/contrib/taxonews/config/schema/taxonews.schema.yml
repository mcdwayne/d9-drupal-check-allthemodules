# Schema for the configuration files of the taxonews module.

taxonews.settings:
  type: mapping
  label: 'Taxonews settings'
  mapping:
    allowed_types:
      type: sequence
      label: 'Vocabularies for which to generate blocks'
      sequence:
        - type: string
          label: 'Vocabulary'
