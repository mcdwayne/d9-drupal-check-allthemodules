services:
  stripe_licensing.webhook_subscriber:
    class: Drupal\stripe_licensing\EventSubscriber\WebHookSubscriber
    arguments: ["@stripe_api.stripe_api", "@entity_type.manager", "@logger.channel.stripe_licensing", "@licensing.licensing"]
    tags:
      - { name: event_subscriber }

  logger.channel.stripe_licensing:
    parent: logger.channel_base
    arguments: ['stripe_licensing']
