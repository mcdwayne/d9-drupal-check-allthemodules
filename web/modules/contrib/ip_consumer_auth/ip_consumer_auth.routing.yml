ip_consumer_auth.consumers_form:
  path: '/admin/config/services/ip_consumer_auth'
  defaults:
    _form: '\Drupal\ip_consumer_auth\Form\ConsumersForm'
    _title: 'IP Consumer Authentication'
  requirements:
    _permission: 'administer IP consumer auth'
