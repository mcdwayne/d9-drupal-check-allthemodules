<?php

/**
 * @file
 * Cache Flusher.
 */

/**
 * Implements hook_page_attachments()
 */
function cacheflusher_page_attachments(array &$attachments) {
  // Attach the modules styling to the module.
  $attachments['#attached']['library'][] = 'cacheflusher/cacheflusher-styling';
}
