# this will install default values in the configuration storage

# Leave it empty but we will make a record of it so it doesn't give warnings
test_api_key:
live_api_key:

# We put the Plugin standard on Live
account_type: Live

# We will set it standard on 30 days (2592000 seconds)
seconds_active: 2592000