# Schema for the configuration files of the AMP Validator SVG module.

amp_validator_svg.settings:
  type: config_object
  label: 'AMP Validator SVG settings'
  mapping:
    warn_only:
      type: boolean
      label: 'Warn only mode'
