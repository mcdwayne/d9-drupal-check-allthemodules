services:
  authentication.jwt:
    class: Drupal\reactify_auth\Authentication\Provider\JsonAuth
    arguments: ['@config.factory', '@user.auth', '@entity.manager']
    tags:
      - { name: authentication_provider, provider_id: 'json_auth', priority: 100 }
