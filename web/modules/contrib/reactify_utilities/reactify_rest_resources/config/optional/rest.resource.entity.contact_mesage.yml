langcode: en
status: true
dependencies:
  module:
    - contact
    - hal
    - jwt
    - serialization
    - user
id: entity.contact_message
plugin_id: 'entity:contact_message'
granularity: resource
configuration:
  methods:
    - POST
  formats:
    - hal_json
    - json
  authentication:
    - jwt_auth
    - cookie
