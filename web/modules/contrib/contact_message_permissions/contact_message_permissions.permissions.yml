bypass contact message access:
  title: 'Bypass contact per-form message access'
  description: 'Allow access to messages for any contact form, regardless of per-form permissions below.'
  restrict access: true

permission_callbacks:
  - '\Drupal\contact_message_permissions\PermissionsGenerator::entityPermissions'
