name: Bulk Alias Checker
description: "Custom module that check whether the alias is exist or not through  CSV import, and CSV format have been attached with this module in sample"
version: "8.x-1.0"
type: module
core: "8.x"
