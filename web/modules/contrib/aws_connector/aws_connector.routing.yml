aws_connector.admin_settings:
  path: '/admin/config/aws/credentials'
  defaults:
    _form: '\Drupal\aws_connector\Form\AWSConnectorForm'
    _title: 'AWS Connector Credentials Settings'
  requirements:
    _permission: 'administer site configuration'
  options:
    _admin_route: TRUE
