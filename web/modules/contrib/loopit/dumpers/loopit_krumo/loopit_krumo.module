<?php

/**
 * Implements hook_page_attachments_alter().
 */
function loopit_krumo_page_attachments_alter(&$page) {
  if (\Drupal::currentUser()->hasPermission('access devel information')) {
    $page['#attached']['library'][] = 'loopit_krumo/krumo_search';
  }
}