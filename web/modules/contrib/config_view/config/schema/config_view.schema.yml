config_view.settings:
  type: mapping
  label: 'Settings for the config view module'
  mapping:
    data:
      type: sequence
      sequence:
        type: string
        label: 'Config entities that should be exposed to Views'
