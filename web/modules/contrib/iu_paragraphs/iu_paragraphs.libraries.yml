iu-grid-builder:
  css:
    component:
      css/iu-paragraphs-grid-builder.css: { }
iu-tabs:
  js:
    # IU Framework uses Foundation 6.2.4.  The Tabs component requires both
    # foundation.util.keyboard.js and foundation.util.timerAndImageLoader.js
    # the former is included with IU Framework 3.x, the later is not.
    #//cdnjs.cloudflare.com/ajax/libs/foundation/6.2.4/plugins/foundation.util.keyboard.js: { type: external, minified: false }
    //cdnjs.cloudflare.com/ajax/libs/foundation/6.2.4/plugins/foundation.util.timerAndImageLoader.js: { type: external, minified: false }
    //cdnjs.cloudflare.com/ajax/libs/foundation/6.2.4/plugins/foundation.tabs.js: { type: external, minified: false }
