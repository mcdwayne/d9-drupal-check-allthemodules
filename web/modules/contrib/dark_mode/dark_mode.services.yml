services:
  theme.negotiator.dark_mode.theme_switch:
    class: Drupal\dark_mode\Theme\DarkModeThemeSwitchService
    arguments: ['@config.factory', '@router.admin_context' , '@theme_handler']
    tags:
      - { name: theme_negotiator, priority: -30 }
