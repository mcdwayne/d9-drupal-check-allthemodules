taxonomy_scheduler.admin_form:
  path: '/admin/config/taxonomy_scheduler'
  defaults:
    _form: '\Drupal\taxonomy_scheduler\Form\AdminForm'
    _title: 'Manage taxonomy scheduler'
  requirements:
    _permission: 'administer site configuration'
  options:
    _admin_route: TRUE
