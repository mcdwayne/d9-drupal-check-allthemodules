name: 'Overlay panel'
type: module
description: 'The module provides a panel with a region for the blocks. The panel can be accessed on every page (admin and front themes) and can be hidden from the screen (called back by pressing a button).'
package: Other
version: VERSION
core: 8.x
