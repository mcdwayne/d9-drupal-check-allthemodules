plugin.plugin_configuration.search_api_processor.hashtag:
  type: mapping
  label: 'Hashtag processor configuration'
  mapping:
    fields:
      type: sequence
      label: 'The selected fields'
      sequence:
        type: string
        label: 'The field name'
    lowercase:
      type: boolean
      label: 'Lowercase'
