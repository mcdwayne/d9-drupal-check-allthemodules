# Schema for configuration files of the Language Hierarchy module.

language.entity.*.third_party.language_hierarchy:
  type: mapping
  label: 'Per language fallback settings'
  mapping:
    fallback_langcode:
      type: string
      label: 'Fallback language code'
