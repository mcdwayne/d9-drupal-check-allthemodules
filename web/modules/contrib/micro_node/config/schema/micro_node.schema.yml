micro_node.settings:
  type: config_object
  label: 'Micro node settings'
  mapping:
    node_types:
      type: sequence
      label: 'The node types we can associate with a site entity'
      sequence:
        type: string
    node_types_tab:
      type: sequence
      label: 'The node types for which display the add form as a local task on the site entity page canonical.'
      sequence:
        type: string
