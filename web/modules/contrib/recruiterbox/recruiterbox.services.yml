services:
  logger.channel.recruiterbox:
    parent: logger.channel_base
    arguments: ['recruiterbox']

  recruiterbox.recruiter_apply:
    class: Drupal\recruiterbox\RecruiterBoxApiService
    arguments: ["@http_client", "@config.factory", "@logger.channel.recruiterbox"]

