dblog_conditions.settings:
  type: config_object
  mapping:
    channels_toggle:
      type: string
      label: 'Send log to DBLog only for specific channels'
    channels_list:
      type: string
      label: 'Listed channels'
