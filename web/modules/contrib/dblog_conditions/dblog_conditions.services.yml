services:
  logger.dblog_conditions:
    class: Drupal\dblog_conditions\Logger\DbLogConditions
    arguments: ['@database', '@logger.log_message_parser', '@config.factory']
    tags:
      - { name: logger }
