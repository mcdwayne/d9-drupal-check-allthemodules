commerce_shipping.commerce_shipping_method.plugin.stepped_by_item:
  type: commerce_shipping_method_configuration
  mapping:
    rate_label:
      type: label
      label: 'Rate label'
    rate_map:
      # TODO: how to specify this as repeated compound value?
      type: map
      label: 'Rate map'
