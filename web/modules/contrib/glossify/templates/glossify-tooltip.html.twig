{#
/**
 * @file
 * Markup for inline tooltips.
 *
 * Available variables:
 * - word: the text where the tooltips applies to.
 * - tip: the tooltip text.
 *
 *
 * @ingroup themeable
 */
#}
<abbr title="{{ tip }}" class="taxonomy-tooltip-tip">{{ word }}</abbr>
