trusted_redirect.admin_form:
  path: '/admin/config/search/trusted_redirect'
  defaults:
    _title: 'Trusted redirect'
    _form: '\Drupal\trusted_redirect\Form\ConfigForm'
  requirements:
    _permission: 'admininister trusted redirect configuration'
