micro_path.settings:
  type: config_object
  mapping:
    entity_types:
      label: Enabled entity types for micro paths
      type: sequence
      sequence:
        type: string
    max_length:
      type: integer
condition.plugin.site_type:
  type: condition.plugin
  mapping:
    site_type:
      type: sequence
      label: Enabled site types
      sequence:
        type: string
