<?php

/**
 * Implements hook_install().
 */
function ui_cache_clear_install() {
  // Flush caches to add new cache tag 'block_id'.
  if (module_exists('block')) {
    cache('block')->invalidateTags(array('content' => TRUE));
  }
}
