ui_cache_clear.settings:
  type: mapping
  label: 'UI Cache Clear settings'
  mapping:
    always_clear_page:
      type: boolean
      label: 'Automaticaly clear current page cache for anonymous users when use UI Cache Clear links'
