# Provides a simple user interface that tells the developer where to go.
asf.description:
  path: 'asf'
  defaults:
    _controller: '\Drupal\asf\Controller\AsfController::description'
  requirements:
    _permission: 'access content'
