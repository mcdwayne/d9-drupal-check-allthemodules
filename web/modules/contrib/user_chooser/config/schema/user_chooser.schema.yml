user_chooser.config:
  type: mapping
  label: 'User Chooser configurtion'
  mapping:
    threshold:
      type: integer
      label: 'Autocomplete threshhold'
    sort:
      type: integer
      label: 'Select widget - order by'
    matching:
      type: sequence
      label: 'Autocomplete widget - match against'
      sequence:
        - type: text
          label: 'Match type'
    format:
      type: text
      label: 'Display format'