<?php

/**
 * Implements hook_cron().
 */
function file_download_token_cron() {
  // Delete all tokens older than 24 hours.
  $download_token_manager = \Drupal::service('file_download_token.download_token_manager');
  $download_token_manager->cleanupTokens();
}
