services:
  serializer.encoder.kml:
    class: Drupal\kml\Encoder\KmlEncoder
    tags:
      - { name: encoder, priority: 10, format: kml }
  serializer.encoder.kmz:
    class: Drupal\kml\Encoder\KmlEncoder
    tags:
      - { name: encoder, priority: 10, format: kmz }
