services:
  cryptolog.middleware:
    class: Drupal\cryptolog\CryptologMiddleware
    arguments: ['@settings', '@cache.backend.chainedfast', '@cache.backend.apcu', '@?cache.backend.memcache', '@?cache.backend.memcache_storage', '@?cache.backend.redis']
    tags:
      - { name: http_middleware, priority: 222 }
