ds.field_template.grid:
  type: mapping
  label: 'The settings of the grid field template'
  mapping:
    no_cols:
      type: integer
      label: 'The number of columns of the grid'
    item_class:
      type: string
      label: 'The CSS class for the item wrapper'
    row_class:
      type: string
      label: 'The CSS class for the row'
    label_tag:
      type: string
      label: 'The HTML tag for the label'
    label_class:
      type: string
      label: 'The CSS class for the label'
    wrapper_class:
      type: string
      label: 'The CSS class for the grid wrapper'
