<?php

use Drupal\wow\Mocks\ServiceStub;

/**
 * @file
 * Dummy module declaring stub services.
 */

/**
 * Implements hook_permission().
 */
function wow_test_permission() {
  return array(
    'wow member permission' => array(
      'title' => t('Member permission'),
    ),
    'wow officer permission' => array(
      'title' => t('Officer permission'),
      'restrict access' => TRUE,
    ),
  );
}

/**
 * Implements hook_init().
 */
function wow_test_init() {
  // Stub the services.
  $services['us'] = new ServiceStub();
  $services['eu'] = new ServiceStub();
  $services['kr'] = new ServiceStub();
  $services['tw'] = new ServiceStub();
  $services['cn'] = new ServiceStub();
}
