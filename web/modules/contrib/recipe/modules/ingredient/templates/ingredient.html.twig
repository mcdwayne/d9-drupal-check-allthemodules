{#
/**
 * @file
 * Default theme implementation to display a ingredient.
 *
 * Available variables:
 * - url: URL of the current ingredient.
 * - name: Name of the current ingredient.
 * - content: All ingredient items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the wrapper.
 * - page: Flag for the full page state.
 * - ingredient: The ingredient entity, including:
 *   - id: The ID of the ingredient.
 * - view_mode: View mode, e.g. 'full', 'teaser', etc.
 *
 * @see template_preprocess_ingredient()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes}}>
  {{ title_prefix }}
  {% if not page %}
    <h2><a href="{{ url }}">{{ name }}</a></h2>
  {% endif %}
  {{ title_suffix }}
  {{ content }}
</div>
