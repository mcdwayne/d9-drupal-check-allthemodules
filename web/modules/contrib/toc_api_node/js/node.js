/**
 * @file
 * Smooth Scroll feature for TOC node.
 */

(function ($) {
  'use strict';

  Drupal.behaviors.toc_api_node_node = {
    attach: function(context, settings) {
        // Your code.
        // console.log('node');.
    }
  };

}(jQuery));
