id: strike
label: Strike-through
name: s
description: Strikes out text.
sample: '[{{ name }}]Deleted[/{{ name }}]'
template_code: '<s>{{ tag.content }}</s>'
dependencies:
  enforced:
    module:
      - xbbcode_standard
