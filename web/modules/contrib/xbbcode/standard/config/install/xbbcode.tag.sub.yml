id: sub
label: Subscript
name: sub
description: Subscripts text.
sample: 'H[{{ name }}]2[/{{ name }}]O'
template_code: '<sub>{{ tag.content }}</sub>'
dependencies:
  enforced:
    module:
      - xbbcode_standard
