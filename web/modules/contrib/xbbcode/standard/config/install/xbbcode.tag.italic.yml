id: italic
label: Italic
name: i
description: Italicizes text
sample: '[{{ name }}]Italic[/{{ name }}]'
template_code: '<em>{{ tag.content }}</em>'
dependencies:
  enforced:
    module:
      - xbbcode_standard
