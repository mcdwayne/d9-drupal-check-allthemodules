<?php

/**
 * @file
 * Contains saudi_identity_field.module..
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function saudi_identity_field_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the saudi_identity_field module.
    case 'help.page.saudi_identity_field':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Saudi identity check field') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements Hook_entity_bundle_field_info_alter().
 */
//function saudi_identity_field_entity_bundle_field_info_alter(&$fields, \Drupal\Core\Entity\EntityTypeInterface $entity_type, $bundle) {
//  $types = [];
//  foreach ($fields as $field) {
//    if($field->getType() == 'saudi_identity_type'){
//      $saudi_identity_type = $field->getSetting('saudi_identity_type');
//      $field->addConstraint('SaudiIdentityCheck');
//    }
//}
//}