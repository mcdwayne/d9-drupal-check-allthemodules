imagepin.pin_widgets:
  path: 'admin/imagepin/pin-widgets/{image_fid}/{field_name}/{entity_type}/{bundle}/{language}/{id}'
  defaults:
    _controller: 'Drupal\imagepin\Controller\ImagepinController::pinWidgetsForm'
    _title: 'Pin widgets'
  requirements:
    _permission: 'pin widgets on images'
