services:
  imagepin.widget_manager:
    class: Drupal\imagepin\Plugin\WidgetManager
    parent: default_plugin_manager
  imagepin.widget_repository:
    class: Drupal\imagepin\WidgetRepository
    arguments: ['@database', '@serialization.phpserialize', '@imagepin.widget_manager', '@current_user', '@entity_type.manager']
