langcode: en
status: true
dependencies:
  config:
    - transaction.type.userpoints_default_points
  enforced:
    module:
      - userpoints_rules_example
id: userpoints_rules_earn_on_content_update
label: 'Earn points for updating content'
transaction_type: userpoints_default_points
description: 'Content updating reward'
details:
  - 'Updates on: [transaction:field_userpoints_default_linkref]'
