name: 'User points rules example'
type: module
description: 'Basic userpoints rules examples. Earn / loss points on user login, register, add / edit contents and posting comments.'
package: User Points
core: 8.x
dependencies:
 - userpoints:userpoints_default
 - rules:rules
 - drupal:comment
 - drupal:node
 - token:token
