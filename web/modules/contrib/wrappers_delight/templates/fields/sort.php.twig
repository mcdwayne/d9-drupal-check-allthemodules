  /**
   * @WrappersDelightMethod(
   *   field = "{{ field_name }}",
   *   type = WrappersDelightMethod::SORT
   * )
   *
   * @param string $direction
   * @param string|null $langcode
   *
   * @return static
   */
  public function {{ method_name }}($direction = 'ASC', $langcode = NULL) {
    return $this->sortBy('{{ field_name }}', $direction, $langcode);
  }
