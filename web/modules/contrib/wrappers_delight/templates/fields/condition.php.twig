  /**
   * @WrappersDelightMethod(
   *   field = "{{ field_name }}",
   *   type = WrappersDelightMethod::CONDITION
   * )
   *
   * @param mixed|null $value
   * @param string|null $operator
   * @param string|null $langcode
   *
   * @return static
   */
  public function {{ method_name }}($value = NULL, $operator = NULL, $langcode = NULL) {
    return $this->byCondition('{{ field_name }}', $value, $operator, $langcode);
  }
