<?php

namespace {{ namespace }};

use Drupal\wrappers_delight\Annotation\WrappersDelightMethod;
use Drupal\wrappers_delight\Annotation\WrappersDelight;
use Drupal\wrappers_delight\WrapperBase;

/**
 * @WrappersDelight(
 *   id = "wrapper:{{ entity_type }}",
 *   type = WrappersDelight::TYPE_ENTITY,
 *   entity_type = "{{ entity_type }}",
 * )
 */
{% if is_abstract %}
abstract class {{ class_name_short }} extends WrapperBase {
{% else %}
class {{ class_name_short }} extends WrapperBase {
{% endif %}

  {% if not is_abstract %}
  /**
   * @inheritDoc
   */
  public static function bundle() {
    return NULL;
  }
  {% endif %}

  /**
   * @inheritDoc
   */
  public static function entity_type() {
    return '{{ entity_type }}';
  }
  
  {{ methods }}
  
}
