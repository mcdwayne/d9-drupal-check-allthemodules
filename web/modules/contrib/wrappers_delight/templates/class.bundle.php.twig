<?php

namespace {{ namespace }};

use Drupal\wrappers_delight\Annotation\WrappersDelightMethod;
use Drupal\wrappers_delight\Annotation\WrappersDelight;
use {{ entity_wrapper_class }};

/**
 * @WrappersDelight(
 *   id = "wrapper:{{ entity_type }}:{{ bundle }}",
 *   type = WrappersDelight::TYPE_BUNDLE,
 *   entity_type = "{{ entity_type }}",
 *   bundle = "{{ bundle }}"
 * )
 */
class {{ class_name_short }} extends {{ entity_wrapper_class_short }} {

  /**
   * @inheritDoc
   */
  public static function bundle() {
    return '{{ bundle }}';
  }
  
  {{ methods }}
  
}
