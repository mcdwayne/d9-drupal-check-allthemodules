<?php

namespace {{ namespace }};

use Drupal\wrappers_delight\Annotation\WrappersDelightMethod;
use Drupal\wrappers_delight\Annotation\WrappersDelight;
use Drupal\wrappers_delight\QueryWrapperBase;

/**
 * @WrappersDelight(
 *   id = "query:{{ entity_type }}",
 *   type = WrappersDelight::TYPE_QUERY_ENTITY,
 *   entity_type = "{{ entity_type }}",
 * )
 */
class {{ class_name_short }} extends QueryWrapperBase {

  /**
   * {{ class_name_short }} constructor.
   */
  public function __construct($bundle = NULL) {
    parent::__construct('{{ entity_type }}', $bundle);
  }

  /**
   * @return \{{ class_name }}
   */
  public static function find() {
    return new self();
  }

  /**
   * @return \{{ entity_wrapper_class }}[]
   */
  public function execute() {
    return \{{ entity_wrapper_class }}::loadMultiple($this->query->execute());
  }
  
  {{ methods }}
  
}
