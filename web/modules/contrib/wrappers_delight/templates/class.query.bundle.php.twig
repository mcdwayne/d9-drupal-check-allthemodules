<?php

namespace {{ namespace }};

use Drupal\wrappers_delight\Annotation\WrappersDelightMethod;
use Drupal\wrappers_delight\Annotation\WrappersDelight;
use {{ entity_query_wrapper_class }};

/**
 * @WrappersDelight(
 *   id = "query:{{ entity_type }}:{{ bundle }}",
 *   type = WrappersDelight::TYPE_QUERY_BUNDLE,
 *   entity_type = "{{ entity_type }}",
 *   bundle = "{{ bundle }}"
 * )
 */
class {{ class_name_short }} extends {{ entity_query_wrapper_class_short }} {

  /**
   * {{ class_name_short }} constructor.
   */
  public function __construct() {
    parent::__construct('{{ bundle }}');
  }

  /**
   * @return {{ class_name }}
   */
  public static function find() {
    return new self();
  }

  /**
   * @return \{{ bundle_wrapper_class }}[]
   */
  public function execute() {
    return \{{ bundle_wrapper_class }}::loadMultiple($this->query->execute());
  }
  
  {{ methods }}
  
}
