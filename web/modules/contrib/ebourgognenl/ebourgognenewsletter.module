<?php

/**
 * @file
 * Contains ebourgognenewsletter.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function ebourgognenewsletter_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the ebourgognenewsletter module.
    case 'help.page.ebourgognenewsletter':
      $output = '';
      $output .= '<h3>' . t("À propos") . '</h3>';
      $output .= '<p>' . t("Permet d'inserer un module d'inscription aux newsletter e-bourgogne.") . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function ebourgognenewsletter_theme() {
  $theme = [];

  return $theme;
}

/**
 * Implements hook_page_attachments_alter() to be able to pass php var to ckeditor javascript plugin.
 */
function ebourgognenewsletter_page_attachments_alter(&$page) {
}
