name: Install Profile Generator
type: module
description: Provides a drush command to create an install profile from a site's current configuration
core: 8.x
package: Custom
# We require 8.3.0 because we depend on the install profile being stored in
# configuration.
dependencies:
  - drupal:system (>= 8.3.0)
