drupal_to_slack.settings:
    path: '/admin/config/drupal_to_slack'
    defaults:
        _form: '\Drupal\drupal_to_slack\Form\SettingsForm'
        _title: 'Drupal to slack settings'
    requirements:
       _permission: 'administer site configuration'