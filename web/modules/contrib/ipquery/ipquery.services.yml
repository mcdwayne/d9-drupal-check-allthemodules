services:
  ipquery.query:
    class: Drupal\ipquery\QueryService
    arguments: ['@database', '@request_stack', '@config.factory']
  ipquery.ip2location.download:
    class: Drupal\ipquery\Ip2LocationDownloadService
    arguments: ['@database', '@config.factory', '@lock', '@state', '@datetime.time', '@date.formatter', '@http_client_factory', '@logger.factory', '@file_system']
