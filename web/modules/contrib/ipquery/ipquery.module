<?php

/**
 * @file
 * ipquery module.
 */

/**
 * Implements hook_cron().
 */
function ipquery_cron() {
  /** @var \Drupal\ipquery\Ip2LocationDownloadService $download */
  $download = \Drupal::service('ipquery.ip2location.download');
  $download->process(['cron' => TRUE]);
}
