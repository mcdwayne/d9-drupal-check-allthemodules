langcode: en
status: true
dependencies:
  enforced:
    module:
      - bigcommerce
name: 'BigCommerce Product Category'
vid: bigcommerce_product_category
description: 'Products are grouped together using a category to make them easier to find and browse for shoppers.'
hierarchy: 0
weight: 0
