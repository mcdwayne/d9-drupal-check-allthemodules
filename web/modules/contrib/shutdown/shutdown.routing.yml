shutdown.settings:
  path: '/admin/config/system/shutdown'
  defaults:
    _form: 'Drupal\shutdown\Form\SettingsForm'
    _title: 'Configure your website shutdown'
  requirements:
    _permission: 'shut down website'
