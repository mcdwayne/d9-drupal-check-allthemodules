<?php

/**
 * @file
 * Custom Entity Pager main file.
 */

/**
 * Implements hook_theme().
 */
function custom_entity_pager_theme($existing, $type, $theme, $path) {
  return [
    'paginate' => [
      'variables' => ['element' => NULL],
    ],
    'paginate_with_titles' => [
      'variables' => ['element' => NULL],
    ],
  ];
}
