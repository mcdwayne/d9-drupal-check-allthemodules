webform_sanitize.webform_sanitize_config:
  path: '/admin/config/webform_sanitize'
  defaults:
    _form: '\Drupal\webform_sanitize\Form\WebformSanitizeConfigForm'
    _title: 'Webform Sanitize'
  requirements:
    _permission: 'administer site configuration'
