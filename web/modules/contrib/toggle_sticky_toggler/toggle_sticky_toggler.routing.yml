entity.node.toggle_sticky_toggler:
  path: '/node/{node}/toggle-sticky'
  defaults:
    _controller: 'Drupal\toggle_sticky_toggler\Controller\ToggleStickyToggler::toggleStickyStatus'
  options:
    _admin_route: TRUE
  requirements:
    _permission: 'use toggle sticky toggler links and buttons'
