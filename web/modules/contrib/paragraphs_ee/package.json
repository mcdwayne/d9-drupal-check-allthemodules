{
  "name": "paragraphs_ee",
  "version": "1.0.0",
  "description": "todo",
  "author": "stBorchert",
  "private": true,
  "devDependencies": {
    "node-sass": "^4.11.0"
  },
  "scripts": {
    "compile": "node-sass --recursive --output=css --output-style=compressed scss",
    "watch": "yarn compile --watch"
  }
}