services:
  block_theme_sync.sync_subscriber:
    class: Drupal\block_theme_sync\EventSubscriber\SyncSubscriber
    arguments: ['@entity_type.manager', '@logger.channel.block_theme_sync']
    tags:
      - { name: event_subscriber }
  logger.channel.block_theme_sync:
    parent: logger.channel_base
    arguments: ['block_theme_sync']
