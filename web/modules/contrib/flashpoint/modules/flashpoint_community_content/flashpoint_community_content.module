<?php

/**
 * @file
 * Contains flashpoint_community_content.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function flashpoint_community_content_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the flashpoint_community_content module.
    case 'help.page.flashpoint_community_content':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Contains the Community Content Entity base.') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function flashpoint_community_content_theme() {
  $theme = [];
  $theme['flashpoint_community_content'] = [
    'render element' => 'elements',
    'file' => 'flashpoint_community_content.page.inc',
    'template' => 'flashpoint_community_content',
  ];
  $theme['flashpoint_community_content_content_add_list'] = [
    'render element' => 'content',
    'variables' => ['content' => NULL],
    'file' => 'flashpoint_community_content.page.inc',
  ];
  return $theme;
}

/**
* Implements hook_theme_suggestions_HOOK().
*/
function flashpoint_community_content_theme_suggestions_flashpoint_community_content(array $variables) {
  $suggestions = [];
  $entity = $variables['elements']['#flashpoint_community_content'];
  $sanitized_view_mode = strtr($variables['elements']['#view_mode'], '.', '_');

  $suggestions[] = 'flashpoint_community_content__' . $sanitized_view_mode;
  $suggestions[] = 'flashpoint_community_content__' . $entity->bundle();
  $suggestions[] = 'flashpoint_community_content__' . $entity->bundle() . '__' . $sanitized_view_mode;
  $suggestions[] = 'flashpoint_community_content__' . $entity->id();
  $suggestions[] = 'flashpoint_community_content__' . $entity->id() . '__' . $sanitized_view_mode;
  return $suggestions;
}