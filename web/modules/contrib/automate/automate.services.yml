#services:
#  # Provides service to configure and attach tracking scripts.
#  automate.tracking:
#    class: Drupal\automate\Tracking\AutomateTracking
#    arguments: ['@current_user', '@entity.manager', '@config.factory', '@plugin.manager.condition']
#    tags:
#      - {name: event_subscriber}
