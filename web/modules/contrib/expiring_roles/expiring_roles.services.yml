services:
  expiring_roles.expiring_roles_manager:
    class: Drupal\expiring_roles\ExpiringRoles\ExpiringRolesManager
    arguments: ['@logger.channel.expiring_roles', '@database']
  logger.channel.expiring_roles:
    parent: logger.channel_base
    arguments: ['expiring_roles']
