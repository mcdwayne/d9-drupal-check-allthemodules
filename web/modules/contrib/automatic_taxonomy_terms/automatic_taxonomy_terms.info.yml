name: Automatic Taxonomy Terms
type: module
description: 'Automatically creates taxonomy terms when entities are being created (and optionally keeps them in sync).'
core: 8.x
package: Taxonomy
dependencies:
  - drupal:hook_event_dispatcher
  - drupal:taxonomy
  - drupal:token
