services:
  edgecast.api:
    class: Drupal\edgecast\EdgecastApi
    arguments: ['@config.factory', '@http_client', '@logger.channel.edgecast']

  logger.channel.edgecast:
    parent: logger.channel_base
    arguments: ['edgecast']
