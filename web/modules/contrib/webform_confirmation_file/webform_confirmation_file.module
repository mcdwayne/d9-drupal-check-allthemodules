<?php

/**
 * @file
 * Streams the contents of a file to a user after completing a webform..
 */

/**
 * Implements hook_theme().
 */
function webform_confirmation_file_theme() {
  $info = [
    'webform_handler_confirmation_file_summary' => [
      'variables' => ['settings' => NULL, 'handler' => NULL],
    ],
  ];

  return $info;
}
