webtrees.settings:
 type: mapping
 label: 'Webtrees module settings'
 mapping:
  database:
   type: mapping
   label: 'Database information'
   mapping:
    use_drupal:
     type: string
     label: 'Use Drupal default database settings'
    driver:
     type: string
     label: 'Device driver'
    host:
     type: string
     label: 'Host address'
    port:
     type: string
     label: 'Port'
    database:
     type: string
     label: 'Database'
    prefix:
     type: string
     label: 'Table Name Prefix'
    user:
     type: string
     label: 'User'
    password:
     type: string
     label: 'Password'
  configuration:
   type: mapping
   label: 'Configuration settings'
   mapping: 
    url:
     type: string
     label: 'Webtrees URL base. Usually /webtrees/'
    enable: 
     type: boolean
     label: 'Enable Webtrees login'
    login:
     type: boolean
     label: 'Log in to Webtrees when logging into Drupal'
    use_webtrees:
     type: boolean
     label: 'Webtree has primary user accounts. Create/sync Drupal user.'
    allow_reverse:
     type: boolean
     label: 'Allow secondary login if account does not exist on primary'
    logging:
     type: boolean
     label: 'Log transactions'
    exclude:
     type: string
     label: 'Exclude users with specified role from Webtrees login'
  role:
   type: mapping
   label: 'Role mapping'
   mapping:
    webtrees:
     type: mapping
     label: 'Drupal roles'
     mapping:
      administrator:
       type: string
       label: 'Administrator'
      manager:
       type: string
       label: 'Manager'
      moderator:
       type: string
       label: 'Moderator'
      editor:
       type: string
       label: 'Editor'
      member:
       type: string
       label: 'Member'
    drupal:
     type: mapping
     label: 'Webtrees roles'
     mapping:
      administrator:
       type: string
       label: 'Administrator'
      manager:
       type: string
       label: 'Manager'
      moderator:
       type: string
       label: 'Moderator'
      editor:
       type: string
       label: 'Editor'
  webtrees_user:
   type: mapping
   label: 'Webtrees user initial settings'
   mapping:
    autoaccept:
     type: boolean
     label: 'Auto accept'
    comment:
     type: string
     label: 'Comment'
    comment_exp:
     type: string
     label: 'Comment exp'
    language:
     type: string
     label: 'Language'
    contactmethod:
     type: string
     label: 'Contact method'
    timezone:
     type: string
     label: 'Timezone'
    verified:
     type: boolean
     label: 'Verified'
    verified_by_admin:
     type: boolean
     label: 'Verifited by administrator'
    visibleonline:
     type: boolean
     label: 'Visible online'
    gedcom:
     type: string
     label: 'Default gedcom id'

