{#
/**
 * @file
 * Default theme implementation for a summary of a square pixels scale effect.
 *
 * Available variables:
 * - data: The current configuration for this effect, including:
 *   - square_pixels: The target number of pixels.
 *   - max_width: The maximum width of the resized image.
 *   - max_height: The maximum height of the resized image.
 *   - upscale: If images larger than their original size can scale.
 * - effect: The effect information, including:
 *   - id: The effect identifier.
 *   - label: The effect name.
 *   - description: The effect description.
 *
 * @ingroup themeable
 */
#}
{% if data.square_pixels %}
  {{ data.square_pixels }}
{% endif %}

{% if data.max_width %}
  {% trans %}
    max width {{ data.max_width }}
  {% endtrans %}
{% endif %}

{% if data.max_height %}
  {% trans %}
    max height {{ data.max_height }}
  {% endtrans %}
{% endif %}

{% if data.upscale %}
  {% trans %}
    (upscaling allowed)
  {% endtrans %}
{% endif %}
