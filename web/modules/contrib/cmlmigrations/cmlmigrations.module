<?php

/**
 * @file
 * cmlmigrations.module
 */

use Drupal\cmlmigrations\Hook\CommerceProductInsert;
use Drupal\cmlmigrations\Hook\CommerceProductVariationInsert;
use Drupal\cmlmigrations\Hook\EntityBaseFieldInfo;
use Drupal\cmlmigrations\Hook\FormMigrationEditFormAlter;

/**
 * Implements hook_form_FORM_ID_alter().
 */
function cmlmigrations_form_migration_edit_form_alter(&$form, $form_state, $form_id) {
  FormMigrationEditFormAlter::hook($form, $form_state, $form_id);
}

/**
 * Hook_entity_insert.
 */
function cmlmigrations_commerce_product_insert($entity) {
  CommerceProductInsert::hook($entity);
}

/**
 * Hook_entity_insert.
 */
function cmlmigrations_commerce_product_variation_insert($entity) {
  CommerceProductVariationInsert::hook($entity);
}


/**
 * Hook_entity_update.
 */
function cmlmigrations_commerce_product_update($entity) {
  // CommerceProductInsert::hook($entity);
}

/**
 * Implements hook_entity_base_field_info().
 */
function cmlmigrations_entity_base_field_info($entity_type) {
  if ($fields = EntityBaseFieldInfo::hook($entity_type)) {
    return $fields;
  }
}
