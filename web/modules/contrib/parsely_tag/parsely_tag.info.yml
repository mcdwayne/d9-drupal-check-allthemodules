name: Parse.ly Tag
type: module
description: Create token-aware Parse.ly tags with per-Content Type configuration.
core: 8.x
package: Search & Metadata
configure: parsely_tag.settings

dependencies:
 - drupal:node
 - token:token
