{#
/**
 * @file
 * Default theme implementation to list user types available for adding users.
 *
 * This list is displayed on the Add user admin page.
 *
 * Available variables:
 * - types: A list of user types, each with the following properties:
 *   - add_link: Link to create a user of this type.
 *   - description: Description of this type of user.
 *
 * @see template_preprocess_user_bundle_user_add_list()
 *
 * @ingroup themeable
 */
#}
<dl>
  {% for type in types %}
    <dt>{{ type.add_link }}</dt>
    <dd>{{ type.description }}</dd>
  {% endfor %}
</dl>
