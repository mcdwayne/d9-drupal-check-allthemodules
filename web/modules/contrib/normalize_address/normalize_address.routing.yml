normalize_address.config_form:
  path: '/admin/config/services/normalize_address'
  defaults:
    _title: 'Normalize Address settings'
    _form: '\Drupal\normalize_address\Form\NormalizeAddressSettings'
  requirements:
    _permission: 'administer content types'
  options:
    _admin_route: true
