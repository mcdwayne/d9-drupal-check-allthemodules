<?php

/**
 * @file
 * Fixture module to generate dummy content.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function data_fixtures_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    case 'help.page.data_fixtures':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('The fixtures module allows you to easily create dummy content.') . '</p>';
      return $output;
  }
}
