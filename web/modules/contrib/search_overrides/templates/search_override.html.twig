{#
/**
 * @file search_override.html.twig
 * Default theme implementation to present Search elevate data.
 *
 * This template is used when viewing Search elevate pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_search_elevate()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('search_override') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
