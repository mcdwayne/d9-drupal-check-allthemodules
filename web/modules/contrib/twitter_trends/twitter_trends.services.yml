services:
  # Simple Twitter service to get the Tweet volume.
  twitter_trends.abraham_service:
    class: Drupal\twitter_trends\Services\TwitterTrends
    arguments: ['@state', '@http_client', '@country_manager', '@logger.factory']
