credentials:
  username: 'basic-auth-user'
  password: 'ChangeThisPassword1.'
flood_protection_enabled: 1
protected_routes:
  # The name of the key, here "user_login" does not matter, example config for the user login:
  # user_login:
    # name: 'user.login'
    # methods:
      # - 'GET'

dependencies:
  enforced:
    module:
    - 'route_basic_auth'
